# JARVIS Daily Tech Briefing — Sunday, August 2, 2026

**Quiet day on your path — no seL4 release or CVE, no new x86/AVX2 CPU kernel, no new quant format, and no on-path edge-model drop in the Aug 1–2 window.** llama.cpp cut ~17 tags (**b10201 → b10218**, Jul 31–Aug 1) with nothing identifiable on your x86 CPU path (the tip, b10218, is `minicpmv46 downsample` — multimodal). One conflicting signal worth recording: derivative articles now assert Aion's open weights "landed in July as planned" — **no model card is observable**, so that claim is unconfirmed and contradicts the direct HF check from Aug 1. No standalone technical lead worth elevating.

---
*De-duplicated against the Jul 26 – Aug 1 briefings. Headers with nothing net-new omitted; each beat checked in the ~Aug 1–2 window (last briefing was Aug 1).*

**seL4 / Microkernels**
- No in-window release, verification result, advisory, or CVE. **16.0.0 / Microkit 2.3.0 / CAmkES 3.13.0 / capDL 0.6.0 / rust-sel4 5.0.0** (22 Jul) stand — the breaking changes that touch your code (ReadRegisters/WriteRegisters now respecting `count`; the x86 `ipiStallCoreCallback` PC-corruption fix; ACPI/MADT hardening; the `restore_vmx` VM-escape you're **not** exposed to) were worked through Jul 27–28; nothing new since. `NUM_NODES=6` + `KernelFastpath=ON` remains outside the verified set, so 16.0.0 stays an evaluate-and-migrate call. The membership items search re-surfaces (Gapfruit, Fraunhofer AISEC, Neutrality) are all covered or out-of-window. Summit is Vancouver **Sep 1–3** (early-bird passed Jul 31). **Redox's July monthly is still unposted** (Jun 30 stands), Genode on **26.05**. [seL4 news](https://sel4.systems/news/) · [Redox news](https://www.redox-os.org/news/)

**Local LLM Inference**
- **llama.cpp advanced b10201 → b10218 (Jul 31–Aug 1, ~17 tags) and nothing identifiable lands on your x86 CPU path.** The tip **b10218 = `minicpmv46 downsample` (#25993)** — multimodal image preprocessing; the Jul-31 burst (b10209–b10216) surfaced no CPU/quant/GGUF work in search. Standing pattern holds: **no new x86 CPU/SIMD/GGUF kernel since the `simd_gemm` scalar-tail fix (b9916)**, and the only recent x86 CPU work remains the VNNI-only Q2_0 dot (#26348, off your Zen+ hardware). Immaterial to your deploy regardless — JARVIS runs its own fused-AVX2 `qdot`/forward. [llama.cpp releases](https://github.com/ggml-org/llama.cpp/releases)

**Quantization**
- **Never-briefed, out-of-window (~mid-Jul), flagged as lineage not news: PolyQ (arXiv 2607.14618) — a CPU-oriented end-to-end quantization co-design for edge CPU LLM inference.** Activation-aware channel-wise bit allocation under a user-set average-bit budget, claiming stable quality scaling from 3–6 bits and 2.4–32.1% perplexity gains over prior methods at a 3-bit target (Falcon-H1-3B, Llama2-13B, Qwen3-32B). On-theme for your CPU-only quantized deploy but a *read, not a lever* — JARVIS runs fixed pre-quantized Q4_K_M GGUFs, and variable per-channel bit allocation would be a new dequant path, the same class of work as the deferred TurboQuant/RotorQuant. Nothing else new in-window (searches re-surfaced only covered KV-cache lineage). [PolyQ](https://arxiv.org/html/2607.14618v1)

**Model Releases**
- **Aion-1.0-Instruct — conflicting signal, resolved to UNCONFIRMED: derivative articles now assert the open weights "landed on Hugging Face in July as planned," but no `microsoft/Aion-1.0-*` model card is observable this run either.** Every checkable source still traces to the Jun-2 Build announcement's "planned for July" language, and the Aug-1 direct check found nothing posted; treat the "landed" claim as promotional restatement until a model card URL exists (the seL4-16.0.0 episode cut the other way — verify at source before flipping). Status stays: **overdue, still watching.** [Windows Report](https://windowsreport.com/microsoft-expands-windows-local-ai-with-new-aion-models-and-on-device-apis/)
- No new dense small/edge weights on your six-family CPU path Aug 1–2. Gemma 4 + Jun-5 QAT GGUFs, Qwen 3.5/3.6, Phi-4, Ministral 3, Bonsai 27B, LFM2.5-230M unchanged; Kimi K3 (Jul 27, ~2.8T MoE) remains off-path.

**Systems / OS**
- **Arch Linux developer Morten Linderud ("Foxboron") resigned Aug 1 after a 10-year run** — a security-team member behind debug packages and the Git migration, leaving **mkinitcpio, nvme-cli, archlinux-keyring, arch-install-scripts** and more needing new maintainers (he continues TPM/Secure Boot work outside Arch). Off your code path (the box runs Ubuntu), but a live example of the single-maintainer supply-chain risk in core plumbing a systems dev depends on. [Phoronix](https://www.phoronix.com/news/Arch-Linux-Foxboron)
- **Linux 7.2-rc6 is not yet cut as of this run** (rc5 was Jul 26; rc6 due ~today US time, which lands Monday AEST) — nothing to diff until it posts.

*"Recent" = ~Aug 1–2, 2026; sandbox date 2026-08-02. Generated by the scheduled `jarvis-tech-briefing` task.*
