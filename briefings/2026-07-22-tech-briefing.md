# JARVIS Daily Tech Briefing — Wednesday, July 22, 2026

**Quiet day across the whole stack — no seL4 release, no new x86/AVX2 CPU kernel, no new quant format, and no edge-model drop in the Jul 21–22 window.** llama.cpp cut exactly one tag past yesterday's b10075: **b10076 (Jul 21 15:52), a CUDA `get_rows` int4-copy vectorization** — GPU path, off your CPU deploy. seL4 15.0.0 stands, Redox hasn't posted its July monthly yet, and the quantization/model wires re-surfaced only already-covered lineage. No standalone lead item today; the one honest, JARVIS-useful nugget is a follow-up to yesterday's Bonsai/1-bit lead, in Local LLM Inference below.

---
*De-duplicated against the Jul 14–21 briefings. Headers with nothing net-new are omitted; each beat checked in the ~Jul 21–22 window (last briefing was Jul 21):*

**seL4 / Microkernels**
- No in-window release, verification, or advisory; no CVE. seL4 **15.0.0** / Microkit 2.2.0 current; SMP + x64 VT-x still *unverified* (your `NUM_NODES=6` + `KernelFastpath=ON` posture); the Proofcraft MCS functional-correctness proof (RISC-V done → AArch64 next, DARPA PROVERS) unchanged; **Summit 2026 (Vancouver Sep 1–3) early-bird cutoff Jul 31 — 9 days out**. Redox still on the **Jun 30** monthly (July monthly not yet posted), Genode on **26.05** — no new post from either. [seL4 news](https://sel4.systems/news/)

**Local LLM Inference**
- **b10076 (Jul 21 15:52) is the only tag past yesterday's b10075** — a CUDA `get_rows` same-type int4-copy vectorization (hoists row-invariant work out of the per-element loop, copies one int4/16 B per thread on the contiguous same-type path). GPU only; **no new x86 CPU/SIMD/GGUF/quant kernel** since the already-briefed `simd_gemm` scalar-tail fix (b9916). [llama.cpp releases](https://github.com/ggml-org/llama.cpp/releases)
- **Follow-up to yesterday's Bonsai/1-bit lead — "no AVX2 kernels included" is true *upstream*, but a reference AVX2 ternary `vec_dot` now exists to study rather than build blind.** A community fork (**OpenTransformer/llama.cpp-prismml**, off PrismML-Eng) carries a "Q1_0_g128 CPU kernel fix + AVX2 SIMD" commit, and the *separate* upstream ternary PR **#22836 (STQ1_0, ARM NEON)** notes an optimized **x86 kernel "on the way"** (x86 falls back to naive C today). This is undated community/in-progress work, **not an upstream landing** — it doesn't change yesterday's call (a future memory-bandwidth lever + a hand-rolled-AVX2-`qdot` exercise, *not* a task), it just means the `qdot` you'd write now has a template to diff against. [PR #22836](https://github.com/ggml-org/llama.cpp/pull/22836) · [fork commit](https://huggingface.co/OpenTransformer/llama.cpp-prismml/commit/03ba2cdf1a8e723f70faba661ecb681d86131162)

**Model Releases**
- No new small/edge weights on your six-family CPU path Jul 21–22. **Kimi K3** open weights land **Jul 27 — 5 days out** (2.8T MoE, data-center-scale — off-path even once out; MoE is a loader/kernel path you don't have). **Microsoft Aion-1.0-Instruct** open weights still unposted ("committed for July" — a HF search returns only unrelated AION models; the one pending edge drop worth a re-check as the month closes). Bonsai 27B (Jul 14), Ministral 3, Gemma 4 + Jun-5 QAT GGUFs, Qwen 3.5/3.6, Phi-4, LFM2.5-230M unchanged.

*Quiet on standalone quantization (searches re-surfaced only the covered lineage — TurboQuant, PolyKV, T-SAR / Litespark ternary-CPU-SIMD, NSNQuant, KV-CAR, the System-Aware KV survey) and on on-path systems/OS (Linux 7.2-rc4 routine; swap-tiering + the Gleixner syscall-entry rework already covered; nothing on your bare-metal x86 CPU path). Headers dropped, not padded.*

*"Recent" = ~Jul 21–22, 2026; sandbox date 2026-07-22. Generated by the scheduled `jarvis-tech-briefing` task.*
