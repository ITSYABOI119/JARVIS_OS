# Deploy history (archived from CLAUDE.md, 2026-09-02)

These are the deployed-state records that CLAUDE.md carried in its phase table from the 6-5 flip (2026-07-21) through the soak deploy (2026-08-13). Each was SUPERSEDED by the next and is a **snapshot**: the numbers, the md5s, the `BootOrder` strings and the bracketed in-place corrections are exactly as recorded at the time and must not be edited to match later truth. Moved here verbatim by `/doctor` so the always-loaded root file carries only the CURRENT deploy record; the headings below were added for navigation and are the only non-verbatim content.
## **DEPLOYED STATE (2026-07-21, commit `a9c1d9a`, boot_id=30): Phase 6 goal 6-5 (control-IN) is COMPLETE and `JARVIS_CONTROL_IN` is FLIPPED DEFAULT-ON — the deployed image is TWO-WAY.** The box now runs, all default-ON: the memory stack (episodic + shared context + retrieval + cache growth), the K self-heal action gate, the always-on monitors, the event-driven wake lane, the ≥5 proactive INFORM behaviors, and the control-IN conversation channel (authenticated HMAC-SHA256 + cross-reboot replay floor + rate limit + the SEC-014 least-privileged input process + the query SHIELD + a signed unicast reply). Deployed image md5 `ba93fe4ecf8b098e1ccbc513bdbb6d76` (verified byte-identical to a rebuild from committed source **[CORRECTED 2026-08-10: that claim was FALSE FOR PROCESS B and is left in place rather than deleted, because it is what the record said at the time. PB was built from `src/ipc/shmem_ipc.c`, a file the script copied nowhere and the repo therefore did not control — see the Shared Memory IPC row. A rebuild from committed source would NOT have reproduced this image's PB. Note the SEPARATE and still-TRUE claims of the form "byte-equal to the box-built binary": those compare the ESP against what the box built, not against source, and are unaffected. Fixed `148551a`; from that commit the phrase is true for both processes. DEPLOYED 2026-08-10: boot_id=52 runs image `244ce42ebb74b61436d01e1529b3c464`, built from `148551a`-inclusive source — from that boot the claim is TRUE again for the DEPLOYED image too.]**); rollback = the retained `.bak-pre65flip` image md5 `379f6bdb2acfa0c685a710f794723bad` and/or dd-zeroing the JKEY slot, both proven; Ubuntu keeps `BootOrder[0]` so control-IN is exposed only while JARVIS is deliberately booted. Honest limits carried forward: the reply is **SIGNED, NOT ENCRYPTED**; the query SHIELD refuses **DEFINED ABUSE CLASSES** and is NOT a general injection detector (injection is contained STRUCTURALLY — K-b: inbound text can never mint an action); third-host non-observation of the unicast reply is **NOT PROVEN**. **[MEASURED 2026-08-10, boot 52 — 0 frames on :51002 at a WIRED promiscuous third host against a 690-frame broadcast positive control; qualified (this switch, most-favourable CAM state); see the boot-52 DEPLOYED STATE amendment.]**



## **DEPLOYED STATE UPDATED 2026-07-25 (commit `f1fc84b`) — the ~12-commit backlog is now ON THE ESP; image md5 `9362c756fb8089ebe07c1b73e69a4a92` (verified byte-identical to the box-built binary), rollback `a865b8302e046d313a707d13bcfed101` (= the 6-6-era image, retained BOTH on the ESP as `.bak-pre-recallfix` and at `~/jarvis_66_image.bak-pre-recallfix`), kernel unchanged at `d22affe8`, Ubuntu keeps `BootOrder[0]` (`0001,0000`). **SHIPPED (ungated, real deployed-path behaviour change):** the `f1fc84b` recall fix (complete-sentence-only preamble, or none — which also changes the default-ON WORKLOAD retrieval lane, the shared-`g3_clean_answer_len` blast radius), `103dea6` model-load fail-closed, `fb1404a` HUD/`[SNAP]` usability status, `7c660ef` the control-IN store-wrap watcher. **SHIPPED INERT:** C/M1b-1/-2 compiled out at `JARVIS_EMBED=0` — teardown VERIFIED (no `JARVIS_EMBED` compile-definition in the build tree; `qmodel_embed_last` absent from the PB objects). All 18 probe/debug flags verified 0 before the build. **KVM GATE PASSED (deploy image, `qemu_test.sh` — its first real outing, and it behaved as intended: `-smp 6` DERIVED from the kernel gen_config, NVMe attached, `-snapshot on`):** 147 `[STATS]` windows to q=14,700 with **`err=0` the only value that ever appears**, model=loaded on all 26 `[SNAP]` lines, NN=6, coherent `[INFER]` prose, **0 `[MODEL-BAD]` / 0 `[ANOMALY]` / 0 `[FAULT]` / 0 real restarts** (the 3 `RESTART` grep hits are the `[RESTART-PB] armed` baseline print, not events). **THE RECALL FIX PROVEN ON seL4 — and proven NOT to be a silent disable, which was the failure mode to rule out:** the `[RETR]` distribution is 6× `hit=0 len=0` (no match) + 8× `hit=1 facts=1 len=0` (exact-key hit whose window holds no completed sentence ⇒ **fragment suppressed, no preamble**) + **6× `hit=1 facts=1 len=157/176/198` (hit WITH a completed sentence ⇒ preamble still BUILT and injected)**. So recall still fires on ~43% of hits and declines only where it would have injected a mid-clause fragment. NOTE the seL4 proof came through the WORKLOAD lane (`main_x86.c:6025`), which exercises the same shared fixed function; the control-IN lane needs an inbound frame and **KVM has no NIC**, so the control-IN-path proof is the bare-metal run. **BARE-METAL VALIDATION NOT YET RUN — the box has NOT been booted into JARVIS on this image; `--bootnext` was deliberately left UNARMED so a later unrelated reboot cannot land in JARVIS unattended. `boot_id` and the before/after on the operator's real stored `0x00AB0C331D611B75` records are therefore UNKNOWN, not implied.** Two debts this deploy structurally CANNOT close, unchanged: the C/M1b-1 **boot-time delta** (needs `JARVIS_EMBED=1`; this image is EMBED=0) and the HUD **`[UNUSABLE]`** panel state (renders only when `g_model_bad` latches, which needs an induced failure — a healthy boot shows `[loaded]` on both old and new images). **SUPERSEDED 2026-07-26 — see the next block.** 

## **DEPLOYED STATE UPDATED 2026-07-26 (commit `a01c1db`, the answer-quality fixes): image md5 `e5eb8b1867b02452ba5974e1390d2606`** (read back off the ESP, byte-identical to the box-built binary), **rollback `9362c756fb8089ebe07c1b73e69a4a92`** (retained BOTH on the ESP as `.bak-pre-thinkingfix` and at `~/jarvis_image.bak-pre-thinkingfix`), kernel unchanged `d22affe8`, Ubuntu keeps `BootOrder[0]` (`0001,0000,0002,0003,0004`), **no `BootNext` armed**. **[CLOSED 2026-07-26 — the `0002,0003,0004` entries DO NOT EXIST; this historical value is left intact because a deploy record is a SNAPSHOT, not a live field.]** Investigated READ-ONLY on the box: `sudo efibootmgr -v` returns exactly TWO entries — `Boot0000* JARVIS seL4` (`\EFI\jarvis\grubx64.efi`) and `Boot0001* ubuntu` (`\EFI\UBUNTU\SHIMX64.EFI`) — with `BootOrder: 0001,0000` and no `BootNext`; and `/sys/firmware/efi/efivars/` holds exactly **two** `Boot####-*` variables, so this is not `efibootmgr` hiding them. **The general fact worth keeping, and the thing that caused the alarm: a `BootOrder` string can list entry numbers whose `Boot####` variables do not exist — a number in an order string is NOT evidence of a persistent entry.** Likely (stated as likely, NOT proven) firmware-generated entries for removable/network devices present during an installer run, reaped by the firmware itself. **No `efibootmgr -B` was run — this is resolved-by-observation, not a fix.** **SHIPPED (all ungated, real deployed-path behaviour change):** `9d70f7f` stop on the model's DECLARED `eos_token_id`; `d8993ed` `<|think|>` moved to the leading system turn + `JARVIS_THINKING` **default 0**, so **the deployed prompt now emits NO think token at all**; `c65e1aa` thought scratch is not recallable + line-leading `"N."` is not a sentence end. **KVM GATE (deploy config, all 16 gate flags verified 0 before building):** `q=300 err=0`, NN=6, model=loaded, 15 coherent `[INFER]`, **0 channel/thought occurrences** (the boot-41 defect is gone), **0 `turn|`/`<eos>` junk**, 0 MODEL-BAD/FATAL/ANOMALY, and `[RETR]` still DISCRIMINATING rather than disabled (4 of 13 hits inject at len 157/176/198, 9 suppressed). **KNOWN REMAINING DEFECT, NOT A REGRESSION: answers still stop MID-SENTENCE at the 50-token cap.** Measured: 5 of 5 realistic questions hit the cap. This deploy fixes the thought-channel garbage, NOT the truncation; the token budget is the binding constraint and is the next milestone (`phase6/docs/ANSWER_QUALITY_DESIGN.md` §11.3, M2). **BARE-METAL VALIDATION NOT YET RUN** — `--bootnext` deliberately left UNARMED, so `boot_id` and the on-hardware before/after are UNKNOWN, not implied. **SUPERSEDED 2026-07-26 by the M2 deploy.** 

## **DEPLOYED STATE UPDATED 2026-07-26 (M2, commit `409213f` + this one): image md5 `e3c0c63761820ead530baec52e517eff`** (read back off the ESP, byte-identical to the box-built binary), **rollback `e5eb8b1867b02452ba5974e1390d2606`** (the thinking-fix image, retained on the ESP as `.bak-pre-m2` and at `~/jarvis_image.bak-pre-m2`), kernel unchanged `d22affe8`, Ubuntu keeps `BootOrder[0]` (`0001,0000`), **no `BootNext` armed**. **SHIPPED (all ungated):** the silent-drop fix (retry the same offset, never advance on a failed send), the six transport ceilings raised in lockstep, and the per-lane cap (workload **50**, control-IN **250** via `MSG_QUERY_LONG` 0x13). **KVM gate on this exact md5:** `q=100 hits=71 infer=13 hb=11 shield=5 err=0` — **workload IDENTICAL to every prior run** — coherent `[INFER]`, 0 probe lines, 0 TRUNCATED/FATAL/MODEL-BAD/ANOMALY; and with the control-IN probe, **219 → 1138 byte answers (5.2×)**. **EXPECT: short answers stay FAST — the cap is a CEILING, not a floor.** Measured in the same run: "explain paging in one line" finished at **27 tokens** (`stop=MODEL-ENDED-TURN`, ~5 s) while "what is a page fault?" used the full **250** (`stop=CAP`, ~46 s). Only explanatory answers approach ~46 s. **BARE-METAL VALIDATION NOT YET RUN for M2 either** — `--bootnext` again left UNARMED. **SUPERSEDED 2026-07-26 by the recall deploy — see the next block.** 

## **DEPLOYED STATE UPDATED 2026-07-26 (the RECALL FIXES, commit `49d797f` + this record): image md5 `61e8142f02cf8a5301d8b7f8b74b302a`** (read back off the ESP, byte-identical to the box-built binary), **rollback `e3c0c63761820ead530baec52e517eff`** (the M2 image, retained BOTH on the ESP as `.bak-pre-recall` and at `~/jarvis_image.bak-pre-recall`, both md5-verified before the swap), kernel unchanged `d22affe8`, Ubuntu keeps `BootOrder[0]` (`0001,0000`), **no `BootNext` armed**. **DEPLOYED-PATH DELTA IS EXACTLY THREE COMMITS** (`git log 9254eff..HEAD -- phase3/src`): `21e427b` comment-only; `0aadb79` the **per-lane recall window** (control-IN 120→256, **workload UNCHANGED at 120**); `49d797f` **`EPI_ACT_CONTROL_IN_LOCAL`=4** so a locally-served answer is never recallable, plus both index sites now CALL `g3_candidate_usable`. **WHAT CHANGES FOR A USER:** measured over the real 62-record store, the median recalled snippet goes **19 B → 168 B** — and the 19 B was `route_decline_text()`'s canned `"I don't track that."`, the most recallable content in the store purely because it fit the old window with its full stop. **KVM GATE PASSED, with its criterion REPAIRED rather than waived.** All 17 probe flags verified 0 pre-build; `err=0` the only value that ever appears, 71/71 `[SNAP]` `model=loaded`, NN=6, 17 coherent `[INFER]`, 0 MODEL-BAD/FATAL/ANOMALY, 0 real RESTART (the `[RESTART-PB] armed` baseline print is not an event), 0 probe lines. **The recorded `[RETR]` 157/176/198 criterion could NOT be met, and the reason was NOT the code:** an earlier gate of mine ran `qemu_test.sh --no-snapshot`, which **mutated the shared KVM fixture** — `~/nvme_test.img`'s workload episodic store rolled to boot 10 / 164,582 lifetime records, destroying the corpus those absolutes described. Proven, not assumed: modelling `epi_index_lookup`'s newest-wins-per-key over the CURRENT image with the SHIPPED C predicts **exactly 7 keys yielding `hit=1 len=0`**, and the run observed exactly 7. **So the absolute was repaired into an A/B on the same fixture** (the C/M1a "GATE 2 was mis-specified → repair it into GATE 2′, don't delete it" precedent): the **pre-change build `9254eff`** — the source of the image being replaced — was rebuilt and run against that same image and produced the **IDENTICAL** distribution (10× `hit=0`, 7× `hit=1 len=0`, 17 `[INFER]`, `err=0`). **That is what the criterion existed to prove: the default-ON workload lane is unmoved.** **GATED, NOT VALIDATED — stated plainly: KVM has no NIC, so the control-IN recall path CANNOT fire there** (the only `[CTRL-RECALL]` line is the boot-time index build, `n=14`). **TWO HARDWARE LEGS ARE OWED and are the operator's to run, and this deploy exists so both ride ONE boot rather than two:** (1) from `0aadb79` — `"explain paging in one line"` should give `[CTRL-RECALL] hit=1 recall=1 len=160` (was `len=0`), **and the load-bearing check is that the ANSWER IS COHERENT PROSE, not that `hit=1`** (a longer preamble is a bigger nudge and `f1fc84b` proved preamble shape can tip Gemma into `<|channel>thought`; `hit=1` with a thought dump is a FAILURE); (2) from `49d797f` — `"how long have you been up?"` → tag 4, `"what is your cpu usage?"` → tag 4, `"what is a page fault?"` → tag 3, read back with `sudo dd if=/dev/nvme0n1 bs=512 skip=21140000 count=4097 | python3 phase3/scripts/parse_episodic.py`. **The live control-IN key fingerprint is `37b420f0`** — start the receiver FRESH or a stale one will sign with the superseded key and the failure will look like a dead box. `JARVIS_THINKING` stays **0** in this image and was never part of it. **SUPERSEDED 2026-07-29 by the front-loading deploy — see the next block.** 

## **DEPLOYED STATE UPDATED 2026-07-29 (T1b, commit `5584275` + this record): image md5 `93a549bc82dabfb09a26eae2c4e7afff`** (READ BACK off `/dev/nvme0n1p4` and byte-equal to the box-built binary — the only proof the ESP holds what was built rather than what was intended), **rollback `61e8142f02cf8a5301d8b7f8b74b302a`** (the recall-fix image, retained in BOTH locations and md5-verified BEFORE the write: on the ESP as `.bak-pre-t1` and at `~/jarvis_image.bak-pre-t1`), kernel unchanged `d22affe8`, Ubuntu keeps `BootOrder[0]` (`0001,0000`), **no `BootNext` remaining** (the one-shot consumed itself and the box returned to Ubuntu by itself). **SHIPPED (both ungated, control-IN ONLY):** #18 the front-loading instruction and #9 verdict-4 truncation reporting. **SUPERVISED BARE-METAL VALIDATION, boot_id=47:** all six operator questions answered, **`[CTRL-IN-STATS] acc=6 drop=0 (parse=0 rl=0 auth=0 replay=0) bp=0 down=0 recall=3`**, `err=0` the ONLY value that ever appears, 766/766 `[SNAP] model=loaded NN=6`, self-test `5/5 (3 real, 2 vacuous)`, and **0 `[FATAL]` / 0 `[MODEL-BAD]` / 0 `[ANOMALY]` / 0 `[RESTART]`**. Six `[CTRL-IN] ACCEPT` records match the six questions in order by length (26/21/69/26/23/21) on strictly ascending seqs. **ZERO `<|channel>`/thought text in any reply** — the §2c ordering (instruction AFTER the recall preamble, immediately before the question) holds on real hardware with a real preamble, which had only ever been shown in KVM. **Store tag split EXACTLY as predicted — 3/3/3/4/4/3** (`CONTROLIN` for the four model-answered, `CTRLIN-LOCAL` for the two PA-rendered SYSFACTS/DECLINE), all `/OK`. **THE VERDICT (the strategist's, NOT the operator's — the operator explicitly DECLINED to judge answer length, and attributing a judgement to someone who did not make one is the same class of error as a commit describing work never done): DEPLOYED, ALL REGRESSION CHECKS CLEAN, ANSWER LENGTH ACCEPTABLE BUT UNDER REVIEW.** The thin-answer risk (caveat 3) is real but NARROWER than feared — it shows on open explanatory/reasoning questions, not on "one line" questions. **Q3 is the weakest**: *"because a single-threaded program can only execute one task at a time"* is correct but CIRCULAR — it restates the question instead of explaining why the extra cores have no independent work to take. **Q6 is the evidence the ceiling is the INSTRUCTION, not the model**: asked twice, the second answer opened byte-identically to the first and then CONTINUED with new detail, which is recall firing on hardware and the model doing exactly what the preamble label asks. **`recall=3`, not 4, and the reason is verified rather than assumed:** of the four INFER questions, Q3's newest prior (`[12:00055]`) is a 256-B store-capped fragment with **ZERO sentence terminators**, so `g3_clean_answer_len`'s complete-sentence rule correctly emitted NO preamble — the suppression working as designed, not a recall failure. **VERDICT 4 IS NOT HARDWARE-EXERCISED — stated plainly rather than implied as validated:** all six real answers ended naturally, so #9 shipped correct and UNEXERCISED on the box; it remains KVM-proven only (a probe-gated `CAPPROBE` clamp forced the CAP stop there). **[CLOSED 2026-08-02 — verdict 4 fired on a GENUINE answer (no probe clamp) at the veto flip, boot_id=49.]** **TWO MEASUREMENT LIMITS OF THE DEPLOY CONFIG, recorded so a later session does not mistake absence for evidence: (a) per-answer `n=` is NOT AVAILABLE** — `[PB] gen` is probe-gated and the boot emitted 0 of them, so the KVM `n≈38` figure for Q2 is NEITHER confirmed NOR refuted on hardware, and it must not be back-inferred from byte lengths; **(b) `[CTRL-IN-REPLY]` / `[CTRL-IN-RESP]` / `[CTRL-RECALL]` / `[ROUTE]` are plain `puts_serial` and are NOT durable at `BOOT_LOG=0`, so the ABSENCE of a `TRUNCATED` line is not evidence of no truncation.** No-verdict-4 is instead established from the answers themselves: Q1/Q2/Q3 stored at 166/186/180 B — all BELOW the 256-B `EPI_RESP_MAX` and all ending in a full stop — and Q6's verbatim reply ends on a complete sentence. **A CORRECTION TO THE "STRICTLY BETTER" FRAMING, since the evidence is weaker than it looks:** the pre-deploy Q2 record `[15:00065]` ends mid-clause, but it measures **exactly 256 B = the STORE cap**, so from the store alone a store-capped record is INDISTINGUISHABLE from a generation-truncated one. What IS provable: the post-deploy Q2 answer is **186 B, under the cap, ending in a full stop — complete as generated**. That the old one ran to the 250-token cap rests on the M2 KVM measurement, not on this record. **TWO PROCESS FINDINGS FROM THE DEPLOY ITSELF: (1) `install_jarvis_x86.sh` HAS NO NON-INTERACTIVE FLAG** (`--yes`/`--force`/`ASSUME_YES`/`NONINTERACTIVE`/`BATCH` all absent), so `guard_esp_device`'s two confirmations (`:524-527` — re-type the partition, then `ADD-JARVIS`) were satisfied BY MACHINE over ssh with **no sanctioned bypass**. The outcome here was sound (exact partition named in the prompt in advance, backups hashed first, read-back md5 matched) — but **a confirmation answered by machine is not a confirmation**: its purpose is to make a human look at the device path AT THE MOMENT OF THE WRITE, and a typo'd partition in a future prompt would no longer be caught. **RECORDED AS A KNOWN GAP; adding a `--yes` flag is its own change with its own review and was deliberately NOT done here.** **(2) `install_jarvis_x86.sh` is `-rwxr-xr-x` in the main-PC repo but `-rw-rw-r--` on the box clone**, so `./phase3/scripts/…` fails there with `command not found`; invoke it via `bash` rather than `chmod`-ing a tracked file (which would leave a spurious mode change in git). 

## **DEPLOYED STATE UPDATED 2026-08-01 (C/M3b — the `JARVIS_EMBED` FLIP; semantic recall is LIVE): image md5 `0b771bba80831cdddef39afd9447c57a`** (READ BACK off `/dev/nvme0n1p4` and byte-equal to the box-built binary — the only proof the ESP holds what was built), **rollback `93a549bc82dabfb09a26eae2c4e7afff`** (the T1b front-loading image, retained in BOTH locations and md5-verified BEFORE the write: on the ESP as `.bak-pre-embed` and at `~/jarvis_image.bak-pre-embed`), kernel unchanged `d22affe86cfd6d91d3bdc9a3c559df02`, Ubuntu keeps `BootOrder[0]` (`0001,0000`), **no `BootNext` remaining** (the one-shot consumed itself and the box returned to Ubuntu by itself). **SHIPPED: `JARVIS_EMBED` 0 -> 1** — the semantic-recall lane (embed-on-write, bounded backfill, exact-key-first recall with the `cplen==0` semantic fall-through) is now DEPLOYED, plus **telemetry v13 for everyone** (the struct is v13 regardless of the flag — only the fill is gated — so this deploy changed the wire format and gave v13 its FIRST on-wire validation). All 14 `*_PROBE` flags verified 0 before the build; the kernel-config gate passed (SMP `NUM_NODES=6`, XSAVE feature-set 7, FXSAVE off, IOMMU off, Fastpath ON). **VALIDATED IN TWO STAGES ON PURPOSE, so a v13 failure could never be confused with a semantic-recall failure. STAGE 1 (v13 + no regression) PASS:** telemetry decoded **v13 / 270 B / crc_ok on real hardware for the first time**, `sem_inited=1` LIVE (not "gated off"), `err=0`, NN=6, **boot_id=48**, 0 `[RESTART]`/`[FATAL]`/`[MODEL-BAD]`, and CACHE_GROWTH/MONITORS/WAKE/PROACTIVE all latched at the first `[STATS]`. **STAGE 2 (semantic recall) PASS:** durable read-back `[CTRL-IN-STATS] acc=13 drop=0 (parse=0 rl=0 auth=0 replay=0) bp=0 down=0 recall=4`, wire counters `sem_recall_hits=4 tried=12 floor=8`, `err=0` the ONLY value that ever appears (1692 occurrences), self-test `5/5 (3 real, 2 vacuous)`, `GGUF loaded OK`, 0 `UNUSABLE`, q climbing past 84,600 with 0 restarts. **ALL 13 ANSWERS VERIFIED CORRECT AGAINST THEIR QUESTIONS — ZERO FALSE RECALL**, which is the single most important result here: false recall is the risk cosine-topk introduces that exact-key structurally could not, and it stayed at the 0-observed rate. **THE HEADLINE NUMBER, AND ITS DENOMINATOR IS THE SUBTLE PART:** the operator's design was **6 fact -> paraphrase PAIRS plus 1 "test" = 13 accepted turns** (verified in the control-IN store, boot 17, records `[17:00072]`..`[17:00084]`, all `CONTROLIN/OK`). Only the 6 *paraphrase* turns are genuine recall opportunities — **a fact-setting turn CANNOT recall, there is nothing stored for that topic yet, it is the turn that CREATES the memory**. So the raw counters read **4 hits / 12 lane entries (33%)** while the paraphrase-opportunity reading is **4 of 6 (67%)**, and the arithmetic closes exactly: 6 fact-turns + 2 paraphrase misses = the 8 below-floor. **Both numbers are recorded because they answer different questions; "4 of 12" understates the rate by counting the memory-creating turns in the denominator.** Honest caveat: **3 of the 4 hits are directly confirmed** by their observed cosines (0.713 / 0.672 / 0.668); **the 4th is INFERRED from the arithmetic and is NOT attributable to a specific turn** — see the observability gap below. Either way n=6 is far too small to move the pre-registered 19/36 measurement. **THE CEILING IS UNCHANGED AND STILL THE ONLY CLAIM: about half of paraphrases recall, and a miss degrades to EXACTLY today's no-preamble path — the answer is simply un-augmented, which is what happens today for every non-verbatim question. NEVER "semantic recall works".** **KNOWN OBSERVABILITY GAP, MEASURED NOT ASSUMED:** at the deployed `JARVIS_DBG_BOOT_LOG=0`, the ONLY durable Phase-C marker is `[CTRL-IN-STATS]` (895 records, carrying `recall=`). `[CTRL-RECALL]`, `[CTRL-SEM]`, `[EMBED-SEM]`, `[EMBED-BACKFILL]`, `[EMBED-WRITE]` and `[TLM-V13]` are plain `puts_serial` and captured **ZERO** — so per-turn recall attribution is impossible off-box, and that is exactly why the 4th hit cannot be named. (`[TLM-V12]` also captured zero, so this is the established pattern for the `[TLM-Vn]` debug lines, not a C/M3a regression.) **This is also the concrete argument that C/M3a was necessary: without v13 on the wire the `sem_*` counters would have been unobservable on this boot entirely** — the operator could only read `hits/tried/floor` because C/M3a put them there. Making `[CTRL-RECALL]` durable (the `recall=` precedent — ride `nvme_log_write`) is the obvious follow-up and is NOT done. **RESIDUAL WAS ZERO THIS RUN:** `tried - hits - floor = 12 - 4 - 8 = 0`, i.e. every attempt resolved to either a hit or a below-floor miss. That does not contradict the three-counter design — the residual is *possible*, not mandatory (it appears on a cold start before backfill completes, or when a match holds no complete sentence) — but it is worth recording that the first real run did not exercise it. Live control-IN key fingerprint verified read-only before handover as **`37b420f0`** (JKEY magic valid, 32 non-zero bytes), matching the receiver's key — the "stale key looks exactly like a dead box" trap was cleared in advance rather than diagnosed after. **SUPERSEDED 2026-08-02 by the VETO FLIP deploy — see the next block.** 

## **DEPLOYED STATE UPDATED 2026-08-02 (C/M4 — the ROUTING-VETO FLIP; the veto is LIVE): image md5 `200052960d547e03683ae974250be8ec`** (READ BACK off `/dev/nvme0n1p4` and byte-equal to the box-built binary — the only proof the ESP holds what was built and gated), **rollback `0b771bba80831cdddef39afd9447c57a`** (the C/M3b image, retained in BOTH locations and md5-verified BEFORE the write: on the ESP as `.bak-pre-vetoflip` and at `~/jarvis_image.bak-pre-vetoflip`), kernel unchanged `d22affe86cfd6d91d3bdc9a3c559df02` (VERIFIED, not assumed), Ubuntu keeps `BootOrder[0]` (`0001,0000`), **no `BootNext` remaining** (the one-shot consumed itself and the box returned to Ubuntu). **SHIPPED: `JARVIS_ROUTE_VETO` 0 -> 1** (one line; `route.c`/`routing_suite.h` byte-identical — the veto wraps `route_classify`'s RESULT, never edits the classifier) **plus telemetry v14 for everyone** (the struct is v14 regardless of the flag — only the fill is gated — so this deploy changed the wire format and gave v14 its FIRST on-wire validation). All 16 `*_PROBE` flags counted mechanically from the header and verified 0 before the build (CLAUDE.md's historical "14" and "17" counts were both stale); `JARVIS_PB_TICK`/`DBG_BOOT_LOG`/`M1_MEASURE`/`KM2A_SPIKE`/`SHIELD_LEARN`/`SEMANTIC`/`THINKING`/`G3_AB` also 0. Kernel-config gate passed (SMP `NUM_NODES=6`, XSAVE feature-set 7, FXSAVE off, IOMMU off, Fastpath ON). **KVM HEALTHY GATE CLEAN:** `[VETO] centroids=OK armed` exactly ONCE and correctly sequenced AFTER the fail-closed `mu=OK` gate, **ZERO `[VETO]` decision lines** (no NIC ⇒ no control-IN; armed-but-unexercised is the honest KVM shape, and `[TLM-V14] veto checked=0 vetoed=0 inited=1` × 33 says exactly that), the workload signature **EXACT to the digit** (`q=100 hits=71 infer=13 hb=11 shield=5 err=0`), `err=0` the only value, 6/6 `[SNAP] model=loaded`, NN=6 ×35, 17 coherent `[INFER]`, 0 FATAL/MODEL-BAD/ANOMALY, fixture unmutated (`nvme_test.img` mtime still Jul 29). **SUPERVISED BARE-METAL VALIDATION, boot_id=49 — ALL 8 PRE-REGISTERED LEGS MATCHED THEIR REGISTRATIONS.** The §2 expectation table was computed and recorded BEFORE the boot (C and Python agreeing on all 7 pre-computable legs), so no outcome could be re-based after the fact. **V1** `how long have you been up?` ×2 → HELD both times with the uptime ADVANCING (`up 37 seconds` → `up 206 seconds`) = the live-state proof; **V2** `what causes page faults?` (margin 0.657) → **VETOED** → *"Page faults are caused when a program tries to access a page of memory that is not currently loaded into physical memory."* — the bare-word defect dying on real hardware; **V3** `how does a transformer model work?` (0.269) → **VETOED** → a real self-attention explanation, NOT `"Gemma 4 E2B"`; **V4** the recorded 40000-faults arithmetic question (**0.398 — THE ONLY GENUINELY-NEW PREDICTION**, never in the parity set) → **VETOED** → an arithmetic attempt, so the host-computed prediction reproduced on the box's Q8_0 weights; **V5** `how many cpus does a raspberry pi have` (0.103) → **HELD** → `"6 workers"`, the WRONG answer PERSISTING exactly as designed (one of the 6 residual FPs, kept deliberately visible); **V6** `what gguf is in use` (**margin 0.004835 — the wobble leg, both outcomes pre-registered**) → **FIRED, MATCHING THE HOST** → a generic model answer = arm C's one measured FN reproduced on hardware (recorded as R4 exercised live; this single decision not flipping is NOT evidence that box-vs-host parity is tight generally — 0.0094 is the MAX delta over 36 pairs); **V7** `what is your cpu usage?` → still DECLINED with the canned `"I don't track that."`; **V8** the operator's IMPROVISED `"what's my favorite color?"` → classified INFER, answered honestly *"As an AI, I do not know what your favorite color is."* (no fabricated preference — consistent with the never-knows-preferences ceiling). **COUNTER RECONCILIATION EXACT, and the arithmetic closes:** wire `answered=9` = `route_sysfacts 3` + `route_decline 1` + `route_infer 5`; **`route_veto_checked=7`** = the 7 SYSFACTS *captures* (V1×2, V2, V3, V4, V5, V6); `7 checked − 4 vetoed = 3 held` = `route_sysfacts 3`; `route_infer 5` = 4 rerouted (V2/V3/V4/V6) + 1 natively-INFER (V8). **THE SYSFACTS-ONLY SCOPE HELD ON HARDWARE, and this is the guard that most mattered:** V7 (a DECLINE capture) and V8 (an INFER capture) BOTH left `route_veto_checked` pinned at 7 — the veto never ran on either, so a no-source metric question was never handed to Gemma to FABRICATE, which is the failure `route_decline_text()` exists to prevent and a class the measurement never priced (host GATE 3's invariance, now proven live). **FIRST ON-WIRE v14:** 1315 captured packets, **every one `version=14` and `crc_ok`**, `veto_inited=1` on all of them, and `q_errors=0` + `restart_count=0` the ONLY values that ever appear; all 9 replies HMAC-verified AND CRC-ok. **DURABLE READ-BACKS (from Ubuntu after the session):** `[CTRL-IN-STATS] acc=9 drop=0 (parse=0 rl=0 auth=0 replay=0) bp=0 down=0 recall=2` — matching the 9 replies exactly, with every drop sub-counter zero; across 2709 durable lines `err=0` is the ONLY err value, `model=loaded` the only model state, `NN=6` the only node count, and **0 FATAL / 0 MODEL-BAD / 0 ANOMALY / 0 UNUSABLE / 0 RESTART**; the box ran **~72 minutes to q=813,600** (far longer than the leg session — 26,597 log entries, ~10 ring wraps). **JACT boot 35 = 9 × `action=5 NOTIFY/EXECUTED/OK risk=0 trigger="control-in answered"` (a FIXED LITERAL) + 1 × `action=4` uptime digest** (`digest up=1h q=668200 err=0 heal=0 mon=0 wake=0 tok=547` — B4 firing on the long run, integer-only as designed). **AUDIT HYGIENE TEETH: every raw-query probe = 0.** One grep for `40000` appeared to hit; investigated rather than reported, it is `[3:00002]` from **boot 3** — `trigger="mon uptime ms=86400292 mark=86400000"`, the digits being a substring of the 24 h millisecond mark — and the real leak tests (`"40000 faults"` / `"My workload"` / `"costs 10ms"`) are all 0. **THE STORE INDEPENDENTLY CONFIRMS THE ROUTING:** the control-IN store (boot 18) recorded **5 `CONTROLIN` (tag 3) = V2/V3/V4/V6/V8 = `route_infer` exactly**, and **4 `CTRLIN-LOCAL` (tag 4) = V1×2/V5/V7 = `route_sysfacts` + `route_decline`**. **A REAL BEHAVIOURAL CONSEQUENCE, PREDICTED BEFORE THE BOOT AND THEN MEASURED: the veto changes what enters the RECALL CORPUS.** A vetoed question is written tag 3 (recallable) where before the flip it would have been SYSFACTS → tag 4 (never recallable) — and the `b566dbc` provenance fields made an emergent chain visible: **V4 recorded `recall=semantic src=87 cos=0.706`, i.e. it matched V2's OWN just-created record** and took that page-fault explanation as its preamble; V8 recorded `recall=exact src=54` (asked in a prior session); V2/V3/V6 recorded `recall=miss` at cos 0.275/0.343/0.340, below the 0.55 floor and degrading to the no-preamble path. That is `recall=2` = 1 semantic + 1 exact, matching the wire's `sem_recall_hits=1`. **TWO HONEST LIMITS RECORDED RATHER THAN GLOSSED. (1) THE FLIP MOVES A QUESTION TO THE MODEL; IT DOES NOT MAKE THE MODEL RIGHT.** V4 was correctly rerouted and still answered **0.05% where 5.56% is correct** — its own shown working was right (400,000 ms → 400 s; 2 h → 7,200 s) and it mislabelled the FRACTION 0.0556 as a percentage, an exact 100× error, before the 250-token control-IN cap truncated it at the division step. So the veto converts *confidently irrelevant* into *relevant but possibly wrong*; the claim is a measured cut in one ROUTING defect class and **NEVER "routing is fixed"** and never anything about answer correctness. **(2) `verdict=4` (answered-but-truncated) FIRED ON REAL HARDWARE FOR THE FIRST TIME** on V4, on a genuine answer with NO probe clamp — closing the carry-forward this file records as KVM-proven-only. The reply was 694 B, well under the 1426 B MTU text ceiling, so the binding constraint was the **250-token cap**, not transport. **CARRIED FORWARD, UNCHANGED: box-vs-host veto parity remains UNMEASURED as a whole** (the parity gate proves host-C == host-Python over 206 queries; boot 49 shows 8 hardware decisions agreeing, including the tightest one, which is encouraging but is not the 206-query comparison). **SUPERSEDED 2026-08-09 by the A9-extractions deploy — see the next block.** 

## **DEPLOYED STATE UPDATED 2026-08-09 (the A9 EXTRACTIONS + the model-bad lane fix): image md5 `5b16dda62a879a99c7872f960051bcf3`** (READ BACK off `/dev/nvme0n1p4` after an unmount/remount — a real device read, not page cache — and byte-equal to the box-built binary), **rollback `200052960d547e03683ae974250be8ec`** (the boot-49 veto image, retained in BOTH locations md5-verified BEFORE the write: on the ESP as `.bak-pre-extractions` and at `~/jarvis_image.bak-pre-extractions`, with the earlier `~/jarvis_image.boot49-RESTORE-TARGET` cross-checking to the same md5), kernel unchanged `d22affe86cfd6d91d3bdc9a3c559df02` (VERIFIED, not assumed), Ubuntu keeps `BootOrder[0]` (`0001,0000`), **no `BootNext` remaining** (the one-shot consumed itself). **`grubx64.efi` was REGENERATED by the installer and came out BYTE-IDENTICAL** (`51dfecf930ee15a6317eda579a2e5b88` before and after) — worth recording because rule 3 only anticipated backing up the rootserver image, and the installer also rewrites grub; the grub backup taken to close that gap turned out unnecessary, and *proving* it was unnecessary is the point. **SHIPPED — three `main_x86.c` commits plus the two modules they link:** `b2f87aa` (A9/1, the control-IN index → `ctrl_epi_index`), `69b70c1` (A9/2, the crash-loop window → `pb_health`), `8e4c5be` (the model-bad lane guard inside `PB_DISPATCH_OK()`), with `cc0dfb9` + `4d423a5` linking the two host-tested modules into PA. All 16 `*_PROBE` flags verified 0 before the build — **and the verification method itself needed fixing: 3 of the 16 defines use TWO spaces, so a single-space `PROBE [1-9]` grep would have MISSED a nonzero probe written that way**; the whitespace-robust check is the one that ran. **SUPERVISED BARE-METAL VALIDATION, boot_id=51:** `err=0` the ONLY value across 1606 boot-51 durable lines, `model=loaded` the only model state, and **0 FATAL / 0 RESTART / 0 MODEL-BAD / 0 ANOMALY**; wire v14 crc_ok with all 13 flags co-live and `veto_inited`/`sem_inited`/`route_inited` all 1. Three control-IN legs, every reply HMAC-verified: **(A)** `what causes page faults?` → verdict 0, the **veto FIRED** (`route_veto_checked` 0→1, `route_vetoed` 0→1 — the bare-word class, reproducing boot 49) and the store recorded **`recall=exact src=87`**; **(B)** `what is a semaphore used for?` (never asked) → verdict 0, the semantic lane entered (`sem_recall_tried` 0→1, `sem_recall_floor` 0→1) and the store recorded `recall=miss cos=482`, correctly below the 0.55 floor; **(C)** `how long have you been up?` → verdict 0 `"up 356 seconds"` from PA state, the veto **CHECKED and HELD** (`checked` 1→2, `vetoed` unmoved). Counters reconcile exactly: `answered=3` = `route_infer 2` + `route_sysfacts 1`; durable `[CTRL-IN-STATS] acc=3 drop=5 (parse=5 rl=0 auth=0 replay=0) bp=0 down=0 recall=1`, the `recall=1` matching leg A alone. **THE HONEST CEILING, STATED RATHER THAN GLOSSED: only ONE of the three commits was actually EXERCISED.** A9/1's index path ran for real; **A9/2 (`pb_health`) and the lane guard (`8e4c5be`) are proven only by NOT FIRING** — `pb_health`'s branches need induced restarts (KVM measured the max reachable window as 1 against a bound of 5) and the lane guard needs `g_model_bad`, which a healthy boot never latches. Inertness is the correct and only available evidence for those two; it must never be written up as though the hardware exercised them. **⚠️ THE PRE-REGISTERED LOAD-BEARING LEG IS INCONCLUSIVE, AND THE REASON IS A PLAN DEFECT WORTH REMEMBERING:** the prediction `[CTRL-RECALL] index built n=86` (computed from the operator's real 96-record store BEFORE the boot: 86 `CONTROLIN/OK` + 9 `CTRLIN-LOCAL` + 1 `CONTROLIN/ERROR`-which-is-also-`resp_len=0`) **could not be observed, because `[CTRL-RECALL]` is a plain `puts_serial` line and the `puts_serial`→`nvme_log_write` capture lives inside `#if JARVIS_DBG_BOOT_LOG`, which is 0 in every deployed image.** MEASURED, not assumed: `grep -c CTRL-RECALL` over the boot-51 durable log = **0**, against 807 `[SNAP]` and 805 `[CTRL-IN-STATS]` from the same windows — the identical mechanism boot 50 measured for `[PANEL]`. **A future prediction of this shape must either target a line that rides `nvme_log_write`, or raise `BOOT_LOG` for that boot.** **[REPAIRED 2026-08-09 (`9c772f8`), FOR THE NEXT BOOT — NOT RETROACTIVELY.** `[CTRL-IN-STATS]` now carries ` idx=<n>`, KVM-gated to agree with `[CTRL-RECALL]` (both read 14 in one run), so the prediction re-runs for free on every future boot. **This does NOT make the boot-51 leg pass — it was INCONCLUSIVE and stays INCONCLUSIVE**; the repair rides the next deploy, and the arithmetic corroboration below is separate evidence that must not be relabelled as an observation.]** **WHAT DOES SUPPORT THE 86, short of the direct observation:** the store arithmetic closes independently — post-boot the store holds **88** usable records, this boot added exactly **2** usable (legs A and B, both tag 3) and **1** non-usable (leg C, tag 4, correctly `CTRLIN-LOCAL`), and 88 − 2 = **86**; and `recall=exact src=87` proves the index WAS built at boot from the pre-boot store and contained the right record (seq 87 is the boot-18 record for that exact question). So the index build is functionally proven and the count is arithmetically corroborated, while the printed count itself remains unobserved. **A WHOLE-LOG GREP THAT NEARLY MISREPORTED THIS BOOT:** the durable log is a 2700-entry ring shared across boots, and a naive `grep -c MODEL-BAD` returned **274** — all of them `[50:`, boot 50's N3 probe boot still resident in the ring. Boot 51 has zero. **Always split durable-log counts by `[boot_id:` before reporting them.** The §7 third-host capture rider did NOT run — the Pi is still off the LAN (ssh timeout), and the prompt says to skip it silently in that case. **SUPERSEDED 2026-08-10 by the IPC-RECONCILE deploy — see the next block.** 

## **DEPLOYED STATE UPDATED 2026-08-10 (the IPC RECONCILE — THE FIRST PROCESS B BUILT FROM COMMITTED SOURCE): image md5 `244ce42ebb74b61436d01e1529b3c464`** (READ BACK off `/dev/nvme0n1p4` after an unmount/remount — a real device read, not page cache — and byte-equal to the box-built binary; the box build REPRODUCED this md5 exactly as predicted BEFORE the write), **rollback `5b16dda62a879a99c7872f960051bcf3`** (the boot-51 A9 image, retained in BOTH locations and md5-verified BEFORE the write: on the ESP as `.bak-pre-ipc` and at `~/jarvis_image.bak-pre-ipc`), kernel unchanged `d22affe86cfd6d91d3bdc9a3c559df02` (VERIFIED, not assumed), grub REGENERATED BYTE-IDENTICAL `51dfecf930ee15a6317eda579a2e5b88` (backup `~/grubx64.efi.bak-pre-ipc`), Ubuntu keeps `BootOrder[0]` (`0001,0000`), **no `BootNext` remaining** (the one-shot was consumed by boot 52; the boot itself was operator-performed — ssh dies while JARVIS runs). **THE DEPLOYED-PATH DELTA vs boot 51's image IS EXACTLY TWO COMMITS** (measured — the only `phase3/src`-touching commits since boot 51's build not already in it): **`9c772f8`** (PA — the durable ` idx=` field on `[CTRL-IN-STATS]`) and **`148551a`** (PB — the ring reconcile; PB ELF `908db9d0850b57d64ee411d86c6c1c89` → `c6eafdd2943e9e31ad3f74a55402db14`), plus build-script gate hardening (`814dbef`/`22b7c43`, the config gate now 10 invariants) that provably changed no kernel byte (the kernel md5 is unchanged). **WHAT THIS DEPLOY IS, stated precisely: PROVENANCE, not a behaviour fix.** PA and PB had compiled DIFFERENT implementations of the shared ring since 2026-03-28; the structural proof says the stale PB was BEHAVIOURALLY IDENTICAL (the stale `.c` read `SHMEM_RING_SLOTS` only in `shmem_ipc_init`, which PB never calls; every hot path reads PA's runtime `header.size=15`) — **so the boot's legs prove NO REGRESSION and cannot prove a fix, because there was no live defect to fix.** The value delivered: the deployed Process B is reproducible from committed source FOR THE FIRST TIME, and the `_Static_assert(sizeof(shmem_ring_t) <= 4096)` again compiles in the one TU that could violate it. **[AND THE RUNTIME GUARDS THAT BACK IT ARE UNEXERCISED — MEASURED 2026-08-11, the first time anyone looked: the `ring->header.size == 0 || > SHMEM_RING_SLOTS` checks in BOTH send (`:69`) and recv (`:109`) are never reached by the 300K-iteration harness. Not a subtle miss — `fuzz_shmem_ipc()` randomises `write_idx`/`read_idx` (`fuzz_harness.c:185-186`) but never `header.size`, and every sub-test calls `shmem_ipc_init()` first, which sets size = 15, so the true outcome is impossible for any input it can produce. So the structural argument that the stale PB was behaviourally identical still stands on `shmem_ipc_init` being the only user of the compile-time slot count — but the runtime guard that would CATCH a bad size has never once fired in test. Judged reachable-worth-fuzzing with the one-sub-test fix — **WRITTEN + MEASURED COVERED 2026-08-11 (the closures sweep): `fuzz_shmem_ipc` sub-test 7 drives corrupted sizes {0, 16, 255} through BOTH send and recv plus a restored-size round-trip positive control (runs LAST, consumes no RNG, so the random modes draw the identical stream), and the instrument re-run has shmem_ipc 20 → 17 gaps with the worth-fuzzing bucket EMPTY across every judged module. One precision correction recorded in the judgement: sub-test 6's full-random fill had been reaching recv's `size>15` outcome all along — the 'never touches header.size' mechanism was true for SEND only, and recv's real gap was the 2^-32 `size==0` outcome.** Separately measured: `shmem_ipc_reset()` has ZERO callers in `phase3` — dead code, flagged for a delete-or-justify decision. **[DECIDED + DELETED 2026-08-11 (closures sweep): zero callers RE-verified before deleting (whole-word grep over .c/.h incl. the vendored tree, with `shmem_ipc_send`'s 20 files as the positive control); definition + declaration removed; both host suites recompile + pass UNCHANGED (test_shmem_ipc 10/10, test_shmem_crc 3/3 — no assertion touched, verified not assumed); the module shrinks to 71 lines / 11 gaps, all defensive-guard. This change compiles into BOTH PA and PB objects → it JOINS the pending-deploy delta and rides the next deploy session.]**]** Pre-flight: all 16 `*_PROBE` flags individually verified 0 (the whitespace-robust check), `JARVIS_PB_TICK`=0, `JARVIS_DBG_BOOT_LOG`=0; the installer's confirmations were fed from a FILE, not a pipe (a pipe makes `${PIPESTATUS[0]}` report printf's status, not the installer's), the partition named in advance, exit 0. **SUPERVISED BARE-METAL VALIDATION, boot_id=52 — THE PRE-REGISTERED `idx=` LEG OBSERVED DIRECTLY FOR THE FIRST TIME** (what `9c772f8` exists for; boot 51's same-shape leg was INCONCLUSIVE and stays so): the control-IN store was read BEFORE the boot — 99 records = 89 `CONTROLIN` + 10 `CTRLIN-LOCAL`, of which **88** pass `g3_candidate_usable` (the one non-usable being `[19:00095] CONTROLIN/ERROR … r=""`) → **predicted `idx=88`; the first durable `[CTRL-IN-STATS]` read EXACTLY `idx=88`**, then climbed 89→90→91 as each accepted turn was indexed live; final line **`acc=3 drop=0 (parse=0 rl=0 auth=0 replay=0) bp=0 down=0 recall=3 idx=91`** — `acc`, `recall` and the index delta close three ways. **HEALTH (every count scoped to `[52:` — the 2700-entry ring held 1372 boot-51 + 1328 boot-52 lines):** `err=0` the ONLY err value, `model=loaded` the ONLY model state, NN=6, **0 FATAL / 0 RESTART / 0 ANOMALY / 0 MODEL-BAD / 0 FAULT / 0 UNUSABLE**, 440 `[SNAP]`. **STORE:** 99 → **102** records, checksum OK; `CONTROLIN` 89→**92** (`/OK` 88→**91**), `CTRLIN-LOCAL` unchanged at 10 — all three turns model-answered and recallable (no SYSFACTS/DECLINE turn this boot). ⚠️ the store header's boot counter reads **21** for this boot — it is NOT the telemetry `boot_id` (52); they are independent counters, and this boot's records are `[21:…]`. **RECALL PROVENANCE, read off the store:** `[21:00099]` (*what causes page faults and how does the OS handle them?*) → **`recall=semantic src=96 cos=0.900`** — the semantic lane matching a record boot 51 itself created, the same emergent chain the C/M4 record describes; `[21:00100]` (*explain paging in one line*) → `recall=exact src=66`, its answer stored COMPLETE at 149 B ending in a full stop; `[21:00101]` (*what is a page fault?*) → `recall=exact src=71` — **and that last one EXERCISES THE A9/1 NO-SHADOW INVARIANT ON HARDWARE for the first time**: the store's NEWEST record under that key is `[19:00095]`, a FAILED turn (ERROR, empty response) that was correctly never indexed, so recall served the newest USABLE record instead of nothing — the exact T10 property, previously host-proven only. **A PREDICTION CORRECTION, recorded rather than glossed:** the pre-boot handover predicted `recall=exact src=87` for *what is a page fault?* — **MIS-DERIVED** (`src=87` is boot 51's record for the DIFFERENT question *what causes page faults?*, a different key); the observed `src=71` is the correct newest indexed record for the asked key. The leg PASSED; the predicted number was wrong. **UNVERIFIED, stated plainly rather than implied: (a) whether the three replies were HMAC-verified at the console, and their TRUE byte lengths** — two of the three answers are stored at exactly **256 B = `EPI_RESP_MAX`**, i.e. STORE-CAPPED, so reply length and chunk count CANNOT be measured from the store and **the multi-chunk ring leg is UNVERIFIED** (it needs the receiver transcript, which this record does not have); **(b) the wire v14 telemetry counters for boot 52 — NOT CAPTURED; (c) self-test 5/5 — `Self-Test:` is plain `puts_serial`, not durable at `BOOT_LOG=0`, and its absence from the durable log is not evidence either way.** The §6 third-host rider did NOT run (`.162` has no ARP entry; skipped per the rider's own skip-silently instruction). **[FALSE — corrected by the same-day amendment below: the rider DID run, on the Pi's WIRED eth0 (`.161`); the `.162` no-ARP check tested only the WiFi address.]** **TWO NAMED GAPS THIS DEPLOY SURFACED, recorded so they exist in the repo rather than only in a working file: (1) the box's `/etc/fstab` mounts `/boot/efi` by UUID `8C10-74C0`, which exists on NO partition of this disk** — the real ESP (`/dev/nvme0n1p4`) is `39C5-9D9A`, and the entry's comment still says the ESP was on `p1`. `nofail` swallows the failure, so `/boot/efi` is silently EMPTY after every reboot and reads exactly like "JARVIS is gone" — worse, it makes the deploy ABORT PATH VACUOUS: a restore written to an unmounted `/boot/efi` lands on the ROOT filesystem, the ESP is untouched, and an md5 of the same path reads back what was just written — a restore that reports success while the ESP still holds the new image. (Booting JARVIS is unaffected — UEFI reads the partition directly, not via a Linux mount. Interim: `sudo mount /dev/nvme0n1p4 /boot/efi`.) Fix queued as its own change. **[FIXED 2026-08-10, same day — verified WITHOUT a reboot. The corrected line, VERBATIM (fstab is box-local, so this record IS its version control): `/dev/disk/by-uuid/39C5-9D9A /boot/efi vfat nofail 0 1`, and the stale curtin comment replaced by `# /boot/efi is the internal ESP /dev/nvme0n1p4 (UUID fixed 2026-08-10; the curtin-era 8C10-74C0 matched no partition and nofail hid the failed mount)`. Backup at `/etc/fstab.bak-pre-uuidfix-20260810`, byte-verified before the edit; the diff is exactly those two lines. RE-MEASURED before writing, not assumed: a full `blkid` sweep found `8C10-74C0` on ZERO devices system-wide (one disk), and `p1` — the partition the old comment named — is the Microsoft-reserved partition with NO filesystem at all, so the comment was doubly wrong. VERIFICATION exercised exactly what boot does: `umount /boot/efi` → `mount -a` → `findmnt` shows SOURCE **`/dev/nvme0n1p4`** (the source device, not merely "something is mounted") → `EFI/jarvis/` visible (`boot/`, `grubx64.efi`) → `systemctl daemon-reload` so systemd's generated mount unit matches the edited fstab. `findmnt --verify`: 0 errors; its warnings are PRE-EXISTING and none concern this entry — including a SIBLING of this same defect class, recorded rather than fixed: the fstab swap entry `/dev/disk/by-uuid/864604cb-…` ALSO matches no existing partition and is also silenced (`nofail`; real swap comes from the separate `/swapfile` entry). The abort-path consequence is closed: a restore write to `/boot/efi/EFI/jarvis/…` now lands on the ESP after any reboot, not silently on the root filesystem. FIRST REAL REBOOT PASSED, same day: the box power-cycled later that afternoon (Ubuntu up 18:02) and `/boot/efi` auto-mounted from `/dev/nvme0n1p4` by fstab alone — the boot path itself now exercised, not merely simulated by `mount -a`.]** **(2) Nothing asserts that what `build_jarvis_x86.sh` copies into an app's source tree is what that app's CMakeLists compiles** — the exact gap that let the ring divergence live for 4.5 months (the script wrote `src/ai/shmem_ipc.c` and nothing compiled it, while the CMakeLists compiled `src/ipc/shmem_ipc.c` and nothing wrote it; either half, detected, exposes the whole). The reverse half fails loudly in cmake; the ORPHAN half is the queued CI check. **[CLOSED 2026-08-10 — `check_copy_orphans.py` + the CI step "Invariant: every copied .c is compiled by the app that receives it" (see the Shared Memory IPC row for what it covers). AND THE REVERSE-HALF CLAIM WAS WRONG AS STATED: cmake fails loudly only on a CLEAN tree — on the box's long-lived tree a STALE sibling satisfies it silently, which is the historical bug itself — so the gate checks BOTH directions, not just the orphan half.]** **[AMENDED 2026-08-10, same day — the Q1–Q5 boot-window measurements existed all along and reached the record only after it was first committed (a relay gap, not a measurement gap); four items resolve, one stays open. (Q3) THE THIRD-HOST RIDER RAN — the "did NOT run" sentence above is corrected in place: the Pi came up mid-session WIRED (eth0 = `192.168.100.161`; the `.162` no-ARP check tested only its WiFi address) and captured on eth0 with PROMISC verified DIRECTLY per §6 — the kernel `promiscuity` counter read 1 during the capture and 0 after (sysfs `flags=0x1103` carrying the `0x100` bit; `ip link`'s formatted flag STRING omits PROMISC and was the unreliable indicator — the kernel state is authoritative). Filtered on the box MAC: **690 frames total; :51000 (broadcast telemetry, the positive control) = 690; :51002 (the claim) = 0; :51001 = 0** — the arithmetic closes, all 690 accounted for by the positive control. The standing limit therefore MOVES: third-host non-observation of the unicast reply is now **MEASURED FOR THIS LAN**, carrying BOTH qualifications, and the second is the one people forget: (a) it is a property of THIS switch, not of JARVIS; (b) the CAM entry for the console was refreshed by the request moments before each reply, so the switch was tested in its MOST FAVOURABLE state and a zero was near-predetermined. It rules out a hub, a mirror port, and routine unicast flooding; it says NOTHING about an adversary who floods the CAM or spoofs ARP. **SIGNED, NOT ENCRYPTED is unchanged.** (Q2) WIRE v14 WAS CAPTURED — UNVERIFIED item (b) CLOSES: sampled live TWICE (q=7,900 and q=16,800, IDENTICAL, so the three turns were complete), `version=14` crc_ok: control-IN `answered=3 blocked=0 dropped=0`; routing `sysfacts=0 decline=0 infer=3 route_inited=1`; **veto `checked=1 vetoed=1 veto_inited=1`** — the veto FIRED once and the arithmetic closes (`checked − vetoed = 0 held = route_sysfacts 0`; `infer 3` = 2 native + 1 rerouted), consistent with the bare-word *faults* capture class boot 49 demonstrated (of the three questions only the multi-chunk one contains "faults"; that attribution is arithmetic plus the classifier's recorded behaviour, not a per-turn observation); semantic recall `hits=1 tried=1 floor=0 sem_inited=1` (the wire-side view of Q4 below); health `err=0`, NN=6, `restart_count=0`, `model_load_pct=100`; `retrieval_hits=0` / `retrieval_latency_us=55`, `episodic_count=8192`, `cache_growth_count=9`, `semantic_fact_count=0`, behaviors/monitors/wakes all 0. Flags confirmed PRESENT: MODEL_LOADED, FB_DRAWABLE, CACHE_GROWTH, ACTIONS, MONITORS, WAKE, PROACTIVE, CONTROL_IN; confirmed ABSENT: **RETRIEVAL** (Q5 below). ⚠️ the middle of the flags list (the span that on every prior healthy boot carries FB_MAPPED/SELFTEST_PASS/MEMORY/CONTEXT) was LOST IN RELAY — whether SELFTEST_PASS was on the wire is unconfirmed, so **UNVERIFIED item (c) (self-test) STAYS OPEN pending that one word; do not infer it from precedent**. (Q1) CONSOLE-SIDE HMAC RESOLVES BY CONSTRUCTION, NOT OBSERVATION — item (a) SPLITS: the three replies rendered at the console during the session, and since 6-5/M4b the receiver DROPS any reply failing crc_ok+hmac_ok BEFORE it can reach the console — rendered ⇒ verified. No receiver transcript was read, so per-reply statuses and TRUE byte lengths remain unmeasured: **the multi-chunk leg stays a floor inferred from the 256 B store truncation, NOT a measurement.** (Q4) THE RECALL-LANE DEVIATION, now named: the prediction said `recall=exact` for the recall leg; the observed `recall=3` = ONE semantic + TWO exact — `[21:00099]` took the SEMANTIC lane (wire: `tried=1 hits=1 floor=0`, the same fact from the other side). Clean mechanism, not a fault: exact-key produced no usable preamble (`cplen==0`) and the lane fell through BY DESIGN, matching `src=96` at cos 0.900 — so this one boot exercised BOTH recall lanes where only one was predicted. (Q5) AN OPEN QUESTION, recorded as a question and nothing stronger: `retrieval_hits=0` with `TLM_F_RETRIEVAL` ABSENT while the rest of the default-ON stack's flags were co-live. With 16 inferences against 14,208 cache hits this boot, a zero is PLAUSIBLE (the workload retrieval lane fires only on the inference lane) — but it is UNVERIFIED; flagged for a later look.]**

## **DEPLOYED STATE UPDATED 2026-08-13 (the SOAK deploy — the box is now running UNATTENDED): image md5 `2c061aecdaf08dd9bd88d7d56061f4f4`** (READ BACK off `/dev/nvme0n1p4` after an unmount + `drop_caches` + remount — a real device read, not page cache — and byte-equal to the box-built binary), **rollback `244ce42ebb74b61436d01e1529b3c464`** (the boot-52 IPC-reconcile image, retained in BOTH locations and md5-verified BEFORE the write: on the ESP as `.bak-pre-soak` and at `~/jarvis_image.bak-pre-soak`), kernel unchanged `d22affe86cfd6d91d3bdc9a3c559df02` (VERIFIED, not assumed), grub REGENERATED BYTE-IDENTICAL `51dfecf930ee15a6317eda579a2e5b88` (backed up to both locations; the same no-op the A9 deploy recorded), Ubuntu keeps `BootOrder[0]` (`0001,0000`), JARVIS is **`Boot0000`**, and the one-shot `BootNext` was CONSUMED by boot 54 so **no `BootNext` remains** — if the box ever reboots it returns to Ubuntu, which ENDS the soak but leaves the box REACHABLE. That trade is deliberate: for an unattended month with no support, recoverability beats maximum uptime — a soak that ends at day 12 with clean evidence beats a box nobody can reach on day 3. **THE DEPLOYED-PATH DELTA IS SEVEN COMMITS, NOT SIX — enumerated BY MEASUREMENT** (`git log 148551a..master -- phase3/src` returns 13, of which 6 touch only `test_*`/`fuzz_*`/`net_stack.*` and NONE of those is compiled into the image): `3fd9148` + `14902e7` (the A9/3 `ctrl_exit` module + its `pa_ctrl_gate` wiring) · `4758ac3` (`shmem_ipc_reset` deleted — compiles into BOTH PA and PB) · `0135c0b` (the `JARVIS_ROUTING=0` `#error` guard — INERT at deploy config) · `9155f3f` (the T6 repair: an empty control-IN generation now replies verdict 3 and is NOT counted answered, plus the `EMPTYPROBE` leg) · `0804413` (comment-only) · `4531435` (the JACT trigger joins the verdict set). **Five carry behaviour change, one is inert, one is comments** — the "six" in the task brief matches no natural grouping of these, and the measurement is what is recorded. **HONEST SCOPE, stated because the soak structurally cannot close it: six of the seven are CONTROL-IN-path work and an unattended soak exercises the WORKLOAD lane, so THE SOAK WILL NOT VALIDATE THEM.** The deciding argument for deploying now anyway was the other one — a deploy must happen eventually, and doing it now means it happens while it can still be verified; the alternative was deploying in a month with no help at all. **PRE-FLIGHT (all measured, none quoted): `JARVIS_CONTROL_IN_PROBE` WAS SET TO 3 in the box clone** — a deploy-config build from that state would have shipped an image INJECTING SYNTHETIC CONTROL-IN FRAMES, unattended, for a month; it was reset to 0 and then **all 16 `*_PROBE` flags were counted mechanically from the header and every one verified 0**, plus `PB_TICK`/`DBG_BOOT_LOG`/`KM2A_SPIKE`/`SHIELD_LEARN`/`SEMANTIC`/`THINKING`/`G3_AB`/`M1_MEASURE`. **The two-space trap is REAL and was CONFIRMED rather than accepted: exactly 3 of the 16 defines separate name from value with TWO spaces** (`AVX2_PROBE:47`, `SMP_PROBE:63`, `G3_PROBE:94`), so a single-space `PROBE [1-9]` grep misses them. All **10** kernel-config invariants held. `/boot/efi` was confirmed mounted FROM `/dev/nvme0n1p4` (not merely "something is mounted"), so the deploy's abort path was real rather than vacuous. The clone's dirty tree was **DISCARDED, NOT STASHED** — three of the five files were already byte-identical to master and the other two held a comment block `4531435` deliberately rewrote, so nothing was lost and **the 59-stash pile did not grow**. **KVM GATE (deploy config, `--snapshot`):** the deploy signature EXACT (`q=100 hits=71 infer=13 hb=11 shield=5 err=0`), `err=0` the ONLY value across **554** `[STATS]` windows, 0 FATAL / 0 MODEL-BAD / 0 ANOMALY / 0 UNUSABLE / 0 TRUNCATED, 0 real restarts (the 3 `[RESTART-PB]` hits are the armed baseline pointer dump), 17 coherent `[INFER]`, NN=6, and **ZERO probe lines — the direct evidence the `PROBE=3` hazard was cleared.** The two non-zero greps were IDENTIFIED rather than assumed: `[VETO] centroids=OK armed` is the ARMING line (`[TLM-V14] veto checked=0 vetoed=0 inited=1` confirms zero decisions — the honest armed-but-unexercised KVM shape, since no NIC means no control-IN), and `[FAULT-WIN]` is the PB `.text`-window stash at spawn. **The shared fixture was NOT mutated — md5 `557afe4d…` AND mtime (2026-07-29) both unchanged.** CI counts VERIFIED UNCHANGED with a duplicate-key-aware parse: `test` **136 named of 137**, `coverage` 3 of 5, `boot-smoke` 7 of 9, no duplicate job keys. **SUPERVISED BARE-METAL VALIDATION, boot_id=54 — every pre-registered signal matched the FRESH boot-53 (16 GB) baseline:** `total_ram_mb` **16,025** · `q_errors` **0** · `restart_count` **0** · wire `version=14` `crc_ok=true` · `selftest_score=5` · `NN=6` · `model_load_pct=100` · `sem_inited`/`route_inited`/`veto_inited` all **1** · `infer_last_tok_x100` **558–561** against the 553–556 band, i.e. ~0.4–0.9% ABOVE it — run-to-run variance in the SAFE direction and nowhere near the ~150 that would have meant the threadpool degraded on the smaller RAM. **THE RAM RISK IS THEREFORE CLOSED: `NUM_NODES=6` and full-rate threaded inference are CONFIRMED on 16 GB** — the open question the RAM swap left behind, since no JARVIS boot had happened since. **A FLAG READING THAT LOOKS LIKE A REGRESSION AND IS NOT, recorded so nobody chases it:** the FIRST read (uptime 47 s, `q_total=8`) showed only **8** flags, missing CACHE_GROWTH/MONITORS/WAKE/PROACTIVE — those latch at the FIRST `[STATS]` window (q=100), which an inference-heavy early boot has not yet reached (the same early-boot honesty the boot-19 record describes). Re-read at uptime 230 s / `q_total=7000`: **all 12 present.** **`RETRIEVAL` is correctly ABSENT, and this CLOSES the boot-52 (Q5) open question** that recorded `retrieval_hits=0` with the flag absent as PLAUSIBLE-but-UNVERIFIED: `g_retrieval_hits` counts NON-EMPTY preambles and `f1fc84b`'s complete-sentence rule suppresses fragments, so a workload lane whose exact-key hits hold no completed sentence honestly reports zero and never sets the flag — absence is the CORRECT post-`f1fc84b` behaviour, not a defect. **PENDING-DEPLOY DELTA (as of 2026-09-03) IS THREE COMMITS, NOT ZERO:** `e78d318` (probe-gated, entirely inside `#if JARVIS_EMBED_PROBE == 4`, inert at deploy config with OFF-object-identity proven); `9168e67`, the provenance widening; and the 2026-09-03 emitted mask. The latter two are deployed-path — `ctrl_epi_write` + the recall site + the record layout, plus the builder's `_ex` split (whose wrapper leaves the workload lane md5-identical on the KVM fixture). **CONTROL-IN ONLY**: `ctrl_epi_write` is defined inside `#if JARVIS_CONTROL_IN_RECALL` and every call site is in `pa_ctrl_gate`; the workload lane writes through `epi_batch_add` and is untouched. Neither is deployed — no ESP write, no bootnext.
