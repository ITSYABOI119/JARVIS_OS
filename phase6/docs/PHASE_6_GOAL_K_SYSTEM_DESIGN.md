# Phase 6 — Goal K / M2 System Design: PB-restart (self-healing) via reuse-in-place

**Status:** 🚧 IN PROGRESS — design authored 2026-07-05 (5-agent code sweep, HEAD `4d77a7e`; citations FRESH); **K/M2a spike RUN 2026-07-05 (§4.1)** — the reuse-in-place DIRECTION is locked, the respawn MECHANIC refined (a `spawn_process_v` re-call aborts in muslc's non-re-entrant per-process init → a `pb_restart_entry` muslc-safe re-entry is required; a follow-up **K/M2a-2** spike + K/M2b implement it). **K/M2a-2 pre-mortem hardening recorded 2026-07-05 (§4.2)** — a 5-agent adversarial pre-mortem hardened the WriteRegisters MECHANIC (ReadRegisters→mutate→WriteRegisters preserving `fs_base`; the full worker launch-triple; the ABI-aligned guarded restart stack; cooperative restart) and CORRECTED the measurement to a 3-axis zero-RESOURCE gate (PB-heap + cslot + untyped flat), all cited to HEAD `2401a6b`, before any box code. **K/M2a-2 IMPLEMENTED + BOX-MEASURED 2026-07-05 (§4.2 RESULT): Strategy A LOCKED** — 16 KVM reuse-in-place respawns, all three zero-RESOURCE axes flat (PB musl-heap pointers byte-identical / PA cslot-delta 0 / untyped subsumed) + coherent Gemma every cycle + `fs_base` preserved + **no musl assert** (the §4.1 dead-end resolved); OFF (flag-0) build byte-identical `[INFER]`, err=0. `pb_restart_entry` + the PA reset sequence are committed gated (`JARVIS_KM2A_SPIKE` default 0) as the K/M2b foundation. **K/M2b-1 DONE 2026-07-05 (§4.2 K/M2b-1 RESULT): the multi-worker respawn is CORRECT on NN=6** — the worker launch-triple (B) + the two-mode probe (I, incl. a MID-DISPATCH restart with workers active mid-`qmatmul`) proven under KVM `-smp 6`: 3-axis flat + all 5 workers reset + rejoin (join completes, no deadlock) + coherent gen across N=8 in BOTH modes, no fault/musl-assert; fully gated ⇒ OFF byte-identical by construction. **G + E LANDED 2026-07-05** (§4.2 K/M2b-2-prereq RESULT: file-scope statics + alloc-free tokenizer + cooperative flag; OFF byte-identical; all 3 restart modes incl. mid-tokenize proven on NN=6). **K/M2b-2 spec CORRECTED 2026-07-06 (STEP-1 docs; §5 A–G, §6, §10, §11-Q2, §12):** a 5-agent pre-mortem found the §5 fault-EP plan rested on a FALSE PREMISE — the fault EP already exists (verified: `process_config_default_simple`→`create_fault_endpoint`=true, PB-main bound @ `SEL4UTILS_ENDPOINT_SLOT`); the gap is the RECEIVER (`seL4_NBRecv`) + net-new worker handlers + the detection redesign + the funnel/latch + the first live `shield_assess(learn=NULL)` + JACT audit + the windowed crash-loop bound. STEP-2 (impl) + STEP-3 (real-crash box gate) are the next increment. No shipped behavior change (`JARVIS_ACTIONS` default-0).
**Prereqs:** the it-acts spine (K/M0 host core + K/M1 linked SHIELD gate, box-verified) — `action_allowlist` / `shield_action` / `action_audit` are compiled into PA and the `ACTION_RESTART_PB` id + `shield_assess` are exercised (probe-gated) today. B1 (self-healing PB restart, `ROADMAP.md` §Backlog) folds into this milestone.
**Mirrors:** `phase4/docs/PHASE_4_M3_THREADPOOL_DESIGN.md`, `phase5/docs/PHASE_5_GOAL6_SYSTEM_DESIGN.md`.

---

## 1. Scope / mission

K/M2 is **the first EXECUTED butler action**: Process A detects that Process B has **faulted** (crashed) or **hung**, `shield_assess`es `ACTION_RESTART_PB`, and — if the trust policy authorizes it — **respawns PB** and resumes inference, writing a durable JACT audit record and bumping `restart_count`. It folds backlog **B1** (self-healing) into the keystone: the mechanism that survives a PB crash without a reboot, which Phase 7's "0 crashes over 30 days" needs but no goal otherwise builds.

Scope (from `PHASE_6_GOAL_K_IT_ACTS.md` K/M2, §6): PA detects → `shield_assess(ACTION_RESTART_PB)` (the spine, already landed K/M0–M1) → **reuse-in-place respawn** → audit (verdict EXECUTED, outcome OK|FAIL) + `restart_count++`. Blast-radius bound (§8, verified): **PB holds NO durable state** — episodic / semantic / SCTX / decision-cache / action-audit stores are all PA-side globals; PB re-derives everything from `argv` + already-mapped frames (PA builds argv `main_x86.c:1424-1440`; PB parses `inference_server.c:419-434,467-470,676-682`). So the worst case of a respawn is what a power-cycle already does, minus the reboot.

## 2. Governing constraint — the forward-only, leaky allocator makes realloc-respawn UNSAFE

The single most important verified fact: **there is no teardown path anywhere.**

- PA's VSpace is bootstrapped with the **leaky** variant: `sel4utils_bootstrap_vspace_with_bootinfo_leaky(&vspace, …)` (`main_x86.c:984-985`) — it never reclaims bookkeeping.
- The allocman is bootstrapped **once, forward-only** from the bootinfo untypeds (`bootstrap_use_current_simple`, `main_x86.c:975-976`) over a fixed **500-page static pool** (`ALLOCATOR_STATIC_POOL_SIZE`, `main_x86.c:340`) + a **100,000-page virtual pool** (`main_x86.c:342`, ~400 MiB of bookkeeping sized for PB's ~230K frame allocs). **Neither pool is ever reset.**
- A repo-wide grep confirms **ZERO** occurrences of `sel4utils_destroy_process`, `vka_free`, `seL4_CNode_Revoke`, `seL4_CNode_Delete`, or any `seL4_Untyped_Retype`-teardown in `phase3/src/sel4/`. PB is spawned exactly once and never destroyed.

**Conclusion (locked):** a naive re-call of the spawn path (realloc + re-retype) would **leak, per restart, the ~758K Gemma model frames (§3) + PB's ~230K ELF-image frames (`main:337`) + every CSpace slot** and exhaust the untyped watermark after a handful of restarts. **Reuse-in-place is mandatory** — either reset PB's threads/state while keeping its frames/caps/mappings (Strategy A), or add a *dedicated revocable untyped* and the net-new `seL4_CNode_Revoke` primitive (Strategy B). §4 decides between them via a spike.

## 3. Resource preserve-vs-teardown boundary (each row verified + cited)

### PRESERVE — PA-owned, must survive PB death (NEVER free)
| Resource | Real location | Note |
|---|---|---|
| ~758K **model frame caps** `model_frame_caps[]` | decl `main:358` (file-scope static); alloc loop `main:2412-2456` (store `:2434`); PA map `:2460` | The durable model handle. **Re-mapping from these caps is the whole point** — reloading 2.9 GiB from NVMe per restart = seconds of downtime + re-runs the 758K cap-copy guard. `model_local` (`:2460`) is a **stack local** (CORRECTED from the sweep — not a global); PA re-derives the mapping from the caps. |
| **3 shared frames** (req ring / resp ring / SCTX pool) | alloc `main:1242-1249`; PA map `:1251-1265`; PB dup+map `:1267-1292` | ⚠️ **The frame caps are LOCALS of `spawn_inference_process()`** — not retained in any global after it returns. **K/M2b must hoist these caps to file scope** (or a persistent PA-owned struct) to re-map into the new PB. |
| **`req_notif_obj` / `resp_notif_obj`** notification objects | `main:1156-1169` (locals of `spawn_inference_process`) | ⚠️ Only the `.cptr`s escape into main()'s `req_notif`/`resp_notif` (`main:2746`); the OBJECTS have no durable owner. **Hoist to file scope** so a respawn rebinds rather than reallocs (realloc leaks the old ones — §2). |
| PA VSpace + fixed-vaddr globals: `shared_request_ring` (`:352`→`:1261`), `shared_response_ring` (`:353`→`:1262`), `g_sctx` (`:204`→`:1298`), `inference_process` (`:354`), allocator globals `vspace/vka/simple/vspace_data` (`:344-347`) | as cited | File-scope statics — PA-owned, survive by construction. `g_sctx_ready` (`:205`) gates SCTX use. |
| The Phase-5 memory stores + decision cache + `g_action_audit` (`:503`) | PA globals | All PA-side; PB never touches them. The audit-write path (`action_audit.c`, callback-driven over PA's own NVMe bounce) can log even while PB is down (§8). |

### TEARDOWN-OR-RESET — PB-side, dies/resets with PB
| Resource | Real location | Note |
|---|---|---|
| PB **TCB / CNode (`CONFIG_SEL4UTILS_CSPACE_SIZE_BITS`=22) / VSpace+PTs / stack / IPC-buffer / ELF frames** | config `main:1228-1234` (`sel4utils_configure_process_custom` — the *_custom variant; default_simple + auth only) | Owned by the `inference_process` sel4utils_process object — **would** be reaped by `sel4utils_destroy_process` (which does not exist in-tree). Strategy A resets these in place; Strategy B revokes+re-retypes them. |
| The **N-1 M3 worker TCBs / stacks / IPC-buffers** | create loop `main:1380-1416` (inside `#ifdef CONFIG_ENABLE_SMP_SUPPORT`, block `:1355-1419`); worker entry `threadpool_sel4.c:39-64` | ⚠️ **Created MANUALLY into a throwaway local `sel4utils_thread_t wt`** (`:1398`) in PB's VSpace/CSpace — **NOT owned by `inference_process`**, so a plain destroy would NOT reap them. **K/M2b must track each worker** (a persistent `g_pb_workers[JARVIS_MAX_WORKERS]`) to `sel4utils_clean_up_thread` them, else they dangle in the freed VSpace. |
| `done` / `wake[]` worker Notification objects + PB-CSpace copies | alloc `main:1373-1392`; caps via `sel4utils_copy_cap_to_process` | Also PA-allocated locals, outside the process object — free/re-copy on respawn. |
| PB globals: `g_pool` (threadpool_sel4.c static BSS) | `threadpool_sel4.c:33` | Re-zeroed by a fresh ELF load; if a reused mapping is kept (Strategy A), must re-run `jarvis_sel4_pool_init` (`:66`). |

**K/M2b prerequisite refactor (independent of the A-vs-B choice):** hoist the 3 shmem-frame caps + the req/resp notification objects to file scope, and promote the worker `wt` handles into a persistent array. These are locals today purely because spawn ran once; a respawn needs durable handles to them.

## 4. Respawn strategy: A vs B + the deciding spike

**Strategy A — reset-in-place (RECOMMENDED PRIMARY).** Keep PB's entire process object (CNode/VSpace/TCB/workers) + all mappings + all copied caps. Reset step: `seL4_TCB_Suspend` PB-main + every worker → PA re-copies the ELF **writable** segments (`.data`) and zeros `.bss` **into the existing PB ELF frames** (PA already has the CPIO ELF image mapped — it resolves `jarvis_sel4_worker_entry` from PB's `.symtab` today, `main:1345+`) → `seL4_TCB_WriteRegisters(PB, PC=ELF entry, SP=fresh stack top)`. **`seL4_TCB_Resume` is NOT part of this step** — it is deferred to §6 step 9/10, AFTER the rings are re-inited and workers re-created (resuming before that would re-enter `main()` against stale rings). **Draws ZERO new untyped** ⇒ unbounded restarts AND no 758K-cap re-copy. **Open questions (spike):** (i) whether `sel4utils` exposes a re-load-into-existing-frames primitive, or PA must hand-roll the per-segment `memcpy(.data)` / `memset(.bss)` from the mapped CPIO ELF (PA has the source; the mechanics are the risk); (ii) because PB re-executes `_start`→`main()` at the reset PC, the **argv strings must still be valid at the reset SP** — under A the caps are preserved (no regeneration needed) but argv/stack integrity at re-entry is itself a spike question (see §11 Q3).

**Strategy B — revocable dedicated untyped (FALLBACK).** Pre-carve ONE untyped backing ALL of PB's per-instance objects (CNode/VSpace/TCB/workers), **outside** the model/shared frames. On respawn: `seL4_CNode_Revoke` (net-new — absent from the tree) + re-retype it (resets that untyped's watermark) → re-run `configure_process_custom` + re-map the model + 3 shared frames + rebuild argv. Standard spawn path, but O(pages) re-map cost + re-runs the null-cap-copy guard 758K× + requires proving the revoke of PB's instance untyped **spares** the model/shared frame caps (they must sit in a *different* untyped).

**The spike (K/M2a — runs before design-lock):** a KVM experiment that repeatedly respawns PB **N×** under each strategy and measures **free untyped before/after** (`simple_get_untyped_count` / walking the untyped list). **A passes iff** untyped is FLAT across N restarts AND PB re-inits + generates coherently. **B passes iff** revoke+re-retype resets the dedicated untyped AND the model/shared caps survive the revoke. **Pick the strategy from the spike measurement, not from this prose.**

## 5. Fault vs hang detection (net-new — verified absent today)

**⚠️ CORRECTED 2026-07-06 (K/M2b-2 5-agent pre-mortem — the original §5 rested on a FALSE PREMISE):**

**The fault EP ALREADY EXISTS.** `process_config_default_simple` → `process_config_create_fault_endpoint` sets `config.create_fault_endpoint = true` (`libsel4utils/include/sel4utils/process_config.h:145-147,184`), so `sel4utils_configure_process_custom` allocates `inference_process.fault_endpoint` and **binds PB-main to it @ `SEL4UTILS_ENDPOINT_SLOT`** (`libsel4utils/src/process.c:465,505-506,606-609`; non-MCS). The gap is NOT "add an EP" — it is the **RECEIVER** (PA never `NBRecv`s the fault) + the net-new **WORKER handlers** (created `cfg={0}` = null handler, `main:1411-1416`) + badging. **§11 Q2 is CLOSED.**

The corrected K/M2b-2 detect→assess→execute→audit design (all `#if JARVIS_ACTIONS`, default 0):

- **A. Fault RECEIPT.** `seL4_NBRecv(inference_process.fault_endpoint.cptr, &badge)` — cap in PA's cspace via the file-scope `inference_process` global (`main:354`), no hoist. **NBRecv, never `seL4_Poll`** (notification semantics won't dequeue an endpoint) and **never `seL4_Recv`** (blocking → PA hangs on iteration 1, the normal no-fault case → box dead every boot). Boot-assert the cap is an Endpoint; read the MessageInfo label (fault tag) + IP for the audit snapshot. **Worker handlers (net-new):** `thread_config_fault_endpoint(cfg, inference_process.fault_endpoint.cptr-in-PB-cspace)` at `main:1411` BEFORE `sel4utils_configure_thread_config` — never post-hoc `seL4_TCB_SetSpace` a live worker (re-supplies cspace/vspace roots → bricks it). **Badging = audit-only** (any fault → whole-PB restart); a single class (PB-main vs worker) suffices, PB-main gets a NONZERO badge (badge 0 == "unknown"). **Poll INSIDE the spin** (the inference 5M-poll `main:3332` + `wait_for_response` `main:1502`), co-located with the existing `jarvis_telemetry_tick()` cadence (do NOT add a kernel entry per `seL4_Yield` — perturbs equal-prio scheduling + the `:1494` Heisenbug). **Loop-drain to empty** each poll (SMP faults queue FIFO); coalesce into ONE restart.
- **B. Faulted-thread STATE.** A real fault leaves the thread **`BlockedOnReply`**. **Suspend-FIRST is MANDATORY** before ReadRegisters/WriteRegisters/Resume (cancels the reply-block; a bare Resume on BlockedOnReply no-ops → silent respawn failure). **Never `seL4_Reply`** the fault — the register-rewrite handles it; drain the fault EP after Resume. Assert `fs_base != 0` (the fault-time read may be clobbered; stamp a known-good value if a real-fault probe shows it clobbered). **STEP-3 RESOLVED (2026-07-06): the real-fault probe shows `fs_base=0x4704d0` PRESERVED on every genuine null-READ fault (PB-main and workers) — the conditional stamp is NOT needed; `ReadRegisters`→mutate-only-`rip`/`rsp`/`rflags`→`WriteRegisters` preserves it correctly.** (A wild WRITE could clobber TLS, but K/M2's fault model is null-READ by design.)
- **C. Detection LOGIC (mis-modeled originally).** **Heartbeat-age:** SUPPRESS the age trigger while `g_infer_active==1` (`main:132`) — an open inference window means PB is provably busy, so age is meaningless; the threshold derives from the **OBSERVED inter-heartbeat gap** (heartbeats are a random ~10% slot, so the healthy gap spans many queries — instrument it first), NOT single-query latency. Age is only a coarse backstop for PB alive-but-silent-while-NOT-mid-query. **Per-query watchdog:** the inference lane's own 5M-poll timeout (`main:3329`) IS the inference-lane hang signal. **ONE shared "consecutive PB-contact failures" counter** fed by all THREE PB-touching timeout sites (inference `main:3393`, heartbeat `main:3572`, shield `main:3591`); the **70% cache lane is NEUTRAL** (must neither advance nor reset it — else a hung PB is masked by cache "successes"); reset only on a genuine PB ACK/RESPONSE.
- **D. Single funnel + re-entrancy.** One `pa_restart_pb(reason)`, guarded by a `g_restart_in_progress` latch (skip fault-poll + hang trigger while set) + a short debounce; the JACT write + `restart_count` bump happen EXACTLY ONCE per resolved event. **Any fault (PB-main OR worker) → the FULL whole-PB restart** (`km2b_reset_workers` + `pb_restart_entry`); the badge is audit-only. §4.2-B's "reconstruct only the faulted worker" is SCOPED OUT (a partial worker restart deadlocks the join).
- **E. The action EXECUTE (first live `shield_assess`).** `shield_assess(ACTION_RESTART_PB, ctx, learn=NULL, learn_cap=0)` — **NOT `g_shield_learn`** (it's `#if JARVIS_SHIELD_LEARN` default-0 → would not compile; and SELF_HEAL ceiling 60 < 80 never risk-blocks anyway). **`trigger_snapshot` from SYSTEM FACTS ONLY** (lane id, missed-count, age_ms, fault tag/badge) — NEVER the in-flight query text: the shield-lane queries are ALL blocklist keywords (`main:3581` `shield_queries[]`), so a query-derived trigger would make `shield_assess` BLOCK its own restart (host-test the trigger builder stays keyword-clean fed each `shield_queries[]`). `trust_policy(TRUST_NOTIFY) → ACT_NOTIFY` = execute + JACT audit + a serial `[RESTART]` line (the user-notify surface is K/M3; do NOT collapse `ACT_NOTIFY`/`ACT_EXECUTE`). Clear `g_infer_active`/`g_infer_t0` on restart (`main:3825` recomputes duty from `t0` — a stale `t0` corrupts the counter). JACT audit: verdict EXECUTED, outcome OK|FAIL (PA-side, survives PB down).
- **F. Crash-loop bound + degraded state.** A **windowed** consecutive-restart counter (SEPARATE from the lifetime `restart_count`): distinguish "restart executed + PB came back healthy" (reset after K sustained-coherent queries — a possible false-positive) from "restart failed / PB re-faulted immediately" (bound tightly). On the bound: set `g_pb_dead` → the loop **STOPS DISPATCHING to PB** (serve cache-only / skip the MSG_QUERY/HB/SHIELD *sends* — never enter the 5M timeout) + a distinct `[FATAL]` + HUD/telemetry state. "Surface + stop" must stop the doomed *sends*, not just the restarts.
- **G. `restart_count` is INTERNAL ONLY** at K/M2b-2 — tracked PA-side, NOT on the wire. The v7 telemetry (`restart_count`/`actions_fired`/`actions_blocked` + receiver/fixture/golden.pcap/console-honesty lockstep) is K/M3.

*(Original §5, SUPERSEDED by A–G above — kept for provenance:)* Today a PB crash is invisible: **no fault endpoint** is configured [FALSE — see the correction above], and a crash/hang manifests only as a **5,000,000-iteration poll timeout** that does `q_errors++` + `goto next_query` and **never restarts** — at **TWO** sites: the inline inference lane AND `wait_for_response()` (heartbeat + shield lanes). `jarvis_uptime_ms()` exists but nothing stamps a last-ACK age.

- **(a) FAULT (deterministic).** Add `process_config_fault_endpoint()` to PB's config (`main:1229-1231`) — one badged EP for PB-main and a distinct badge per worker so PA can identify the faulting thread. **PA is a busy-poll loop**, so poll the fault EP **non-blocking** (`seL4_Poll` / a `seL4_NBRecv` on the EP) once per workload iteration. *Decision:* poll-in-the-loop over a dedicated fault-handler thread — a handler thread would need its own priority/affinity + a lock-free flag into the single-writer PA loop, adding concurrency surface for no latency benefit (PA already iterates at high frequency). Justify + confirm in K/M2a.
- **(b) HANG (heuristic).** Stamp `last_hb_ack_ms = jarvis_uptime_ms()` on every `MSG_HEARTBEAT_ACK`. Trigger a restart when the age exceeds a threshold with **margin above the worst-case single-query latency** (~12 s Gemma @ `NUM_NODES=6`): require **N consecutive missed heartbeats spanning > one inference window** so a legitimately BUSY PB (mid-generation, counters frozen — the existing `[STATS]` behavior) is never misread as hung. Both timeout sites feed the same trigger, or a PB hung only on the hb/shield lane won't restart.

The trigger, once fired, calls `shield_assess(ACTION_RESTART_PB, ctx)` → `trust_policy` → execute. **`trust_policy()` is host-tested but never yet called live** (K/M1 is assess-only, "not executed at M1", `main:2246`) — **K/M2 introduces the first live `shield_assess→trust_policy→execute` chain.**

## 4.1 K/M2a spike result (2026-07-05, KVM) — direction LOCKED, mechanic REFINED

**What ran:** a throwaway gated spike (`JARVIS_KM2A_SPIKE`, since reset) exercised the *convenient-primitive* variant of Strategy A — after PB came up (model probed OK, forward pass OK, `[JARVIS] Process B ready type=4 seq=0`), PA suspended PB + workers, re-inited both rings, and **re-invoked `sel4utils_spawn_process_v` on the live `inference_process`** (reusing its CNode/VSpace/TCB/ELF frames/caps).

**Result — it ABORTS on the FIRST respawn cycle, in the C runtime:**
```
[SPIKE] K/M2a reuse-in-place respawn spike START
Assertion failed: ret == boot_set_tid_address
  (.../seL4_libs/libsel4muslcsys/src/vsyscall.c: init_syscall_table: 227)
seL4 root server abort()ed  →  Debug halt syscall from user thread "8"  →  halting
```

**Root cause (the finding neither this design nor the 5-agent sweep anticipated):** muslc's **per-process init** (`init_syscall_table` → `set_tid_address`) is a **ONE-TIME, non-re-entrant** setup whose guard state lives in PB's `.data`/`.bss`. Re-entering PB at `_start` **without resetting those segments** re-runs that init against dirty guards → the assertion. So the naive "re-call `spawn_process_v`" is NOT a valid reset-in-place — it re-enters through the C runtime's init, which cannot run twice.

**§11 Q3 ANSWERED:** reset-in-place CANNOT reuse the naive `spawn_process_v` re-call. It requires **either** (a) re-copying the ELF **writable** segments (`.data` reset + `.bss` zero — which *resets* muslc's init guards so `_start` may safely re-run), **or** (b) a dedicated **`pb_restart_entry`** in `inference_server.c` that re-enters PB **PAST** musl's one-time init (skipping `_start`'s C-runtime setup) and re-derives config from the fixed vaddrs (`SHMEM_VADDR_B` / `MODEL_VADDR_B` — no argv). **(b) is the cleaner path** — it avoids the ELF `.data` re-copy entirely and is the "restart-entry-skips-argv" approach.

**Measurement not obtained *in K/M2a*:** the abort halted the run before completing a single cycle, so the zero-resource gate was not confirmed here. **→ OBTAINED in K/M2a-2 (§4.2 RESULT, 2026-07-05): all three axes flat across N=16, Strategy A LOCKED.**

**DECISION:**
- **Direction LOCKED = reuse-in-place (the Strategy A family).** Strategy B is NOT chosen — it needs net-new `seL4_CNode_Revoke` + a proof that revoking PB's instance untyped spares the model/shared caps, strictly *more* work than fixing the re-entry.
- **Mechanic REFINED:** the respawn re-entry is **NOT** a `spawn_process_v` re-call — it must be a **muslc-init-safe re-entry (`pb_restart_entry` preferred)**. This also simplifies §6 (no re-run of musl's runtime init; the g_pool re-init happens inside `pb_restart_entry`, not via `_start`).
- **Follow-up:** **K/M2a-2** — a second spike implementing `pb_restart_entry` (+ the ELF-writable-reset fallback) and re-measuring the untyped delta across N≥8 cycles — precedes K/M2b.
- **KEPT from this milestone:** the §3 **hoist refactor** (durable file-scope `g_pb_*` handles — real K/M2b prep, OFF-build-verified byte-neutral). The throwaway spike block + its flag were reset (they did not reach a clean gate-passing state).

### K/M2a-2 implementation plan + a second finding (reconnaissance done 2026-07-05)

The `pb_restart_entry` re-entry (§4.1) was scoped against the live `inference_server.c`. A **second finding** shapes it: PB's query loop uses `qm`/`state`/`vocab`/`tok` as **`main()` locals** (`inference_server.c:512-546`, passed into `handle_query` at `:742-744`), and `llama_alloc_state` (`:547`) allocates the KV cache from **PB's bounded heap** (musl `malloc`, heap in `.bss`/`.data` which the restart *preserves*). Therefore a `pb_restart_entry` that **re-loads** the model would **re-alloc ~40 MiB of KV per cycle and leak it** (the old allocation is never freed — the heap is preserved, not reset), exhausting PB's heap within a few restarts. **So the mechanic must REUSE the warm model state, not reload it.**

**Required shape (K/M2a-2, an invasive PB-side change — its own careful increment):**
1. **Hoist** the warm inference state to reachable scope so `pb_restart_entry` can reuse it without re-alloc: `qm` / `vocab` / `tok` / `state` + the config (`req_notif` / `resp_notif` / `request_ring` / `response_ring` / `g_sctx_pb` / pool params `n_threads`/`done`/`wake[]`). Simplest safe form: under `#if JARVIS_KM2A_SPIKE`, stash pointers to `main()`'s locals into file-scope globals after setup + extract the query loop (`:708-765`) into a `pb_serve_loop()` both `main()` and `pb_restart_entry` call.
2. **Dedicated restart stack** (a static `g_restart_stack[]` in PB `.bss`): PA re-enters via `WriteRegisters(PC=pb_restart_entry, SP=g_restart_stack top)` so the new frame does NOT clobber `main()`'s frame (which holds the reused `qm`/`state`); the stashed pointers stay valid. Re-entering at `main()`'s original SP would overwrite that frame — do not.
3. `pb_restart_entry` re-inits ONLY volatile state (`jarvis_sel4_pool_init` from preserved params; per-query KV memset already happens in `handle_query`), re-signals `MSG_HEARTBEAT_ACK` + `Signal(resp_notif)`, and calls `pb_serve_loop()`.
4. PA side: the §6 reset sequence (suspend PB+workers → worker `WriteRegisters(PC=worker_entry)` + re-supply `ipc_buf` → `shmem_ipc_init` both rings, NOT sctx → drain notifs → `WriteRegisters(PB, PC=pb_restart_entry, SP=restart-stack)` → resume → drain-then-poll ready handshake), **with an `alloc_calls` counter around the whole path** (the definitive zero-untyped proof) + one inference per cycle for coherence.

**Status:** reconnaissance + design complete; the implementation is a delicate multi-round KVM bring-up (WriteRegisters PC/SP/arg conventions for PB-main AND workers, the restart-stack, the warm-state reuse) touching the deployed inference path — deferred to its own box-verified increment rather than rushed. `JARVIS_ACTIONS` / deploy image unaffected.

## 4.2 K/M2a-2 pre-mortem hardening (mandatory before box)

A 5-agent adversarial pre-mortem of the §4.1 recon plan found ~8 box-bricking landmines — each would hang the box on cycle 1. The **direction holds** (reuse-in-place); the **WriteRegisters MECHANIC and the MEASUREMENT were under-specified.** Every item below is re-verified to live source (HEAD `2401a6b`) with its fix; the citations that reached this session partly garbled were re-checked (the line numbers were essentially right — e.g. the worker launch really is `main_x86.c:1425-1427`). The work lands behind a re-added `JARVIS_KM2A_SPIKE` (default 0); `JARVIS_ACTIONS` stays default-0 (deploy inert).

### A. Register discipline — THE root fix (`ReadRegisters` → mutate ONLY needed fields → `WriteRegisters`; never a synthesized context)
- **Landmine:** a zero-initialized / hand-built `seL4_UserContext` drops `fs_base`/`gs_base` → PB-main's muslc TLS (errno, the `__sel4_ipc_buffer` pointer, thread-pointer) is unreachable → fault on first TLS access; a stale `DF=1` violates the SysV entry invariant (string ops assume forward).
- **Evidence:** **greenfield** — a repo-wide grep of `phase3/src` for `seL4_TCB_WriteRegisters`/`seL4_TCB_ReadRegisters` returns **ZERO** hits (no prior pattern to copy — get it exactly right once). The muslc/TLS fragility is already visible in the worker path: workers were started OUTSIDE sel4runtime's per-thread init, so they manually `seL4_SetIPCBuffer` before any `seL4_Wait` (`threadpool_sel4.c:41-45`).
- **Fix:** for EVERY thread re-entry, `seL4_TCB_ReadRegisters(thread,…)` FIRST, mutate only the needed fields, then `seL4_TCB_WriteRegisters`. PB-main: set `rip=pb_restart_entry`, `rsp=<restart-stack top, see C>`, `rflags &= ~(1u<<10)` (clear DF) — the read-back **preserves `fs_base`/`gs_base`**. Assert/log `fs_base != 0` after the write, before Resume. `seL4_TCB_Suspend` is idempotent (no suspend-count) → ONE `Resume` suffices (§6 step 9/10). WriteRegisters does NOT touch FPU/XSAVE, so MXCSR/AVX state is preserved by the kernel context-switch (see Verified-CLEAR).

### B. Worker re-entry = the FULL launch triple (not just a PC)
- **Evidence:** the original launch is `sel4utils_start_thread(&wt, (…)worker_entry, arg0=remote_wake[i], arg1=i, resume=1)` (`main_x86.c:1425-1427`) into `jarvis_sel4_worker_entry(arg0=wake, arg1=idx, ipc_buf)` (`threadpool_sel4.c:39`; proto `threadpool.h:53-55`). `sel4utils_start_thread` supplies the **IPC buffer as the auto 3rd entry arg (`rdx`)** — on a raw `WriteRegisters` re-entry PA must supply it ITSELF.
- **Fix:** per genuinely-faulted worker set `rip=g_pb_worker_entry`, `rdi=g_pb_remote_wake[i]`, `rsi=i`, `rdx=<that worker's ipc_buf vaddr>` (preserved in `g_pb_workers[i]` — STEP 2 confirms the exact `sel4utils_thread_t` field, `ipc_buffer_addr`/`initial_stack_pointer`, against the box's libsel4utils), `rsp=<its stack top, ABI-aligned per C>`, `fs_base` preserved, DF clear; re-apply `seL4_TCB_SetAffinity(_, i)` (`main_x86.c:1423`). **Do NOT blanket-restart healthy idle workers** — one parked on `seL4_Wait(wake)` (`threadpool_sel4.c:48`) is fine; only reconstruct a faulted one (K/M2b's per-worker fault-EP badge identifies which; for the K/M2a-2 probe the fault is induced in PB-main while workers are quiescent, item I).

### C. The restart stack — sized, aligned, GUARDED
- **Evidence:** PB-main is the dispatcher (worker 0) and runs the AVX2 matmul work items ITSELF (`threadpool_sel4.c:100-104`), so peak depth is the WHOLE `pb_restart_entry → pb_serve_loop → handle_query → qmodel_forward → jarvis_parallel_for → fn` chain — the budget the sel4utils-default **guarded** PB-main stack (`process_config_default_simple`, `main_x86.c:1246`; no custom stack size set → `CONFIG_SEL4UTILS_STACK_SIZE`) was sized for.
- **Fix:** `RSP = (stack_top rounded DOWN to 16) − 8`. The SysV AMD64 ABI (§3.2.2) guarantees `%rsp ≡ 8 (mod 16)` at a function's first instruction (a normal `call` pushed an 8-byte return address onto a 16-aligned stack); a **16-aligned** RSP makes the compiler's `vmovaps` to a supposedly-16-aligned stack slot **#GP-fault**. **Prefer a vka-allocated guard-paged stack** (allocated ONCE at spike-arm, reused across all N restarts → does not break the per-restart-zero gate; the guard page catches PB-main's deep AVX2 overflow). If `.bss`: `_Alignas(64) char g_restart_stack[<full PB-main budget>]`, bracketed by a zero canary checked each cycle + a stack high-water fill/scan reported to the gate.

### D. `pb_restart_entry` linkage + non-return
- **Landmine:** a file-static entry is DCE'd at -O2 → `resolve_pb_symbol → 0` → `WriteRegisters(PC=0)` → unrecoverable fault.
- **Evidence:** mirror `jarvis_sel4_worker_entry` — external linkage (`threadpool.h:55`), resolved from PB's `.symtab` by `resolve_pb_symbol(elf,size,"jarvis_sel4_worker_entry")` (`main_x86.c:1379`) with a fail-loud `worker_entry == 0` guard (`main_x86.c:1380-1381`).
- **Fix:** `pb_restart_entry` = **external linkage + `__attribute__((used, noinline))` + `_Noreturn`** with an explicit `for(;;)` trap tail (no `ret` into a stackless frame). Add a PA-startup assertion `resolve_pb_symbol(g_pb_elf, g_pb_elf_size, "pb_restart_entry") != 0` (fail-loud, like the worker guard) and confirm the symbol survives `--gc-sections`.

### E. Cooperative restart + allocation-free serve path (closes the arena-tear / stranded-lock window)
- **Landmine:** the serve path `malloc`s every query (`tokenizer_encode` `tokenizer.c:92` → `sp_text = malloc` `:126`, `syms = malloc` `:149`, frees `:228-229`). An async `seL4_TCB_Suspend` landing mid-`malloc` tears musl's arena / strands its non-recursive malloc lock → the restarted `tokenizer_encode` self-deadlocks on the first query.
- **Fix (two parts):** (1) make restart **cooperative** — PB self-checks a `please_restart` flag at the TOP of `pb_serve_loop` (a quiescent point, never mid-malloc) and self-jumps to `pb_restart_entry`; reserve PA's async `Suspend` for the genuinely-wedged / already-faulted case only. (2) **preallocate the tokenizer scratch** (`sp_text`/`syms`, sized to the bounded max query length) once at load so the serve path is allocation-free — which ALSO removes any malloc from the restart-critical path.

### F. PB-side notification drain (PA cannot reach these caps)
- **Evidence:** PA's `wake[]`/`done` Notification caps were `spawn_inference_process()` locals (gone after it returned); only the **PB-CSpace** cptrs escaped to file scope (`g_pb_remote_wake[]`/`g_pb_remote_done`, `main_x86.c:1128-1132`) — those are valid in **PB's** CSpace, so PA cannot `seL4_Poll` them. seL4 Notifications COALESCE (`threadpool_sel4.c:108-112`), so a stale wake left pending desyncs the M3 join.
- **Fix:** move the wake/done drain **INTO `pb_restart_entry`** — PB polls its own `g_pool.wake[1..n]`/`g_pool.done` to empty via `seL4_Poll` (PB-side caps) BEFORE the first dispatch; then explicitly re-zero `g_pool.active/next_idx/gen` (`jarvis_sel4_pool_init` does the struct-field reset, `threadpool_sel4.c:75-79`, but that is SEPARATE from draining the kernel pending-bits on the notification objects — do BOTH). PA still drains its OWN `resp_notif` at the §6-step-9 handshake (that cap it does hold).

### G. Full `qm`/`state` hoist (not a pointer into `main()`'s frame)
- **Evidence:** `qm`/`vocab`/`tok`/`state` are `main()` locals (`inference_server.c:512/524/533/546`), passed into `handle_query` (`:742`); `llama_alloc_state` (`:547`) allocs the ~40 MiB KV cache from PB's **bounded musl heap** (`.bss`/`.data`, which the restart PRESERVES) → a `pb_restart_entry` that RELOADS would leak ~40 MiB/cycle → heap exhaustion in a few restarts (§4.1's second finding).
- **Fix:** hoist `qm`/`state`/`vocab`/`tok` to **file-scope statics** in `inference_server.c` (small structs — the ~40 MiB KV + `fwd_scratch` live behind pointers, so the statics merely move stack→`.bss` at ~zero cost; OFF-build stays inert). Extract the query loop (`:710-765`) into a `pb_serve_loop()` that BOTH `main()` and `pb_restart_entry` call. This deletes the frame-liveness fragility (no reliance on `main()` never returning / the compiler keeping its frame live).

### H. Ordering (happens-before, load-bearing)
- **Fix:** `shmem_ipc_init` BOTH rings (allocation-free — memsets the mapped page + stamps `SHMEM_MAGIC`, pattern `main_x86.c:1280-1281`) **AND** drain the notifications **BEFORE `seL4_TCB_Resume`.** A "drain-then-poll AFTER resume" can consume PB's FRESH `MSG_HEARTBEAT_ACK` (`inference_server.c:689-690`) inside the drain window → a false "PB never ready" timeout. Do **NOT** `sctx_init` (preserve PA working memory, §6 step 4). Also clear the PA latch `g_infer_active`/`g_infer_t0` (`main_x86.c:132/134`; set `:3128-3129`, cleared `:3648-3650`) — a mid-inference crash otherwise leaves a phantom in-flight inference / fake throughput on the console.

### I. Non-corrupting fault probe
- **Fix:** the induced fault MUST be a **volatile null-READ** at a known PB-main point — **NEVER a wild write** (a stray write corrupts the `.data` the restart PRESERVES → unrecoverable). Fire it while workers are QUIESCENT (blocked on `seL4_Wait(wake)`, `threadpool_sel4.c:48`). The mid-dispatch-hang case is an explicit LATER increment (K/M2b) — do not conflate it with the K/M2a-2 clean-crash probe (open question §11 Q4).

### The CORRECTED measurement (STEP 3 gate — `alloc_calls` alone is WRONG)
The ~40 MiB/cycle leak (G) lives in PB's **musl heap**, invisible to PA's untyped counter. The gate is **zero-RESOURCE across N ≥ 8 cycles on ALL THREE axes**:
1. **PB-heap-flat** — latch PB's morecore/`sbrk(0)` break at first-ready, re-report it each restart in the `[RESTART]` line, assert byte-identical. Cheapest proxy: assert `state.key_cache` / `qm.layers` / `vocab.tokens` pointers UNCHANGED (any re-alloc moves them).
2. **cslot-flat** — a counter asserting ZERO `vka_cspace_alloc` / `vka_cnode_copy` / `sel4utils_copy_cap_to_process` in the reset path (Strategy A = pure reuse; any cap re-copy leaks CSpace slots the untyped counter can't see).
3. **untyped-flat** — the `alloc_calls` counter (free-untyped delta; the original §4.1 metric, still necessary but NOT sufficient).

Plus: **coherent generation after each cycle** + **stack high-water headroom** (C). The `[RESTART]` durable line dumps `&qm`/`&state`/`state.key_cache`/heap-break for log-reconstructability (NVMe log). Do NOT grep the coherence check for `[SCTX-PB]`/`[RETR-PB]` — those one-shot guards persist across the reuse-in-place restart, so they won't re-emit (benign).

### Verified-CLEAR (the pre-mortem checked these — do NOT spend effort)
- Warm `llama_state` self-cleans per query (the per-inference KV memset in `handle_query`).
- MXCSR/AVX preserved by the XSAVE context-switch (WriteRegisters doesn't touch FPU/XSAVE — item A).
- stdio is a non-issue (`puts_serial` is a direct MMIO/IPC path, no buffered-stream re-entrancy).
- PA in-flight bookkeeping abandons cleanly (type-matched drain + ring re-init, §6 steps 3/6).
- pool counters self-heal per dispatch (`jarvis_parallel_for` re-arms `next_idx/active/gen` every call, `threadpool_sel4.c:96-98`).
- The SSM conv/ssm-state memset is DeltaNet-conditional — irrelevant for the deployed Gemma; noted for a future Qwen3.5-class model.

### K/M2a-2 RESULT (2026-07-05, KVM) — Strategy A LOCKED

The hardened `pb_restart_entry` + PA reset sequence were implemented behind `JARVIS_KM2A_SPIKE` (default 0) and driven on KVM (single-core: `numNodes=1`, so `pb_n_threads=1` serial — the parked-worker reuse path is exercised trivially; a multi-worker box run is a K/M2b confirmation). **N=16 reuse-in-place respawns, all three zero-RESOURCE axes FLAT + coherent gen + no fault:**

- **PB musl-heap FLAT.** The warm-state heap pointers were **byte-identical across all 16 cycles** (and the armed baseline): `key_cache=0x0623d020`, `value_cache=0x0533c020`, `logits=0x07342020`, `layers=0x004a7000`. Zero re-alloc ⇒ the ~40 MiB KV is reused, not leaked (the §4.1 second-finding hazard is closed). This is the dominant axis.
- **PA cslot-delta = 0 every cycle.** A finding refined the metric: **the allocman hands out DESCENDING cslots**, so the bracket-probe reads `probe_b = probe_a − 1` (consecutive) ⇒ `|Δ|−1 = 0` — the reset path (suspend → ring re-init → notif drain → ReadRegisters/WriteRegisters/Resume → drain-then-poll ACK) allocated **zero** cap-slots. Cross-cycle continuity (each cycle's probes step down by exactly 2 = only the 2 probes) proves the inter-cycle inference allocated zero cslots too. Because every kernel object needs a cap-slot, cslot-flat **subsumes untyped-flat**.
- **Coherent generation every cycle** — identical greedy Gemma output (`"The seL4 microkernel is a **formally verified, high-assurance m…"`), `resp=OK` ×16.
- **`fs_base` preserved (`kept`) every cycle** — the ReadRegisters→mutate-only-rip/rsp/rflags→WriteRegisters discipline (§4.2 A) kept PB-main's TLS; **no musl assert** (the §4.1 `init_syscall_table` dead-end is resolved by re-entering PAST musl init).
- **Stack high-water = 6600 B of 262144** (canary intact, no overflow) — the AVX2 `handle_query→qmodel_forward→parallel_for` chain fits with vast headroom.
- **Zero aborts / faults / canary corruption** across the run; it then fell through to the normal workload (`err=0`, retrieval live).

**OFF leg (deployed-path safety, `JARVIS_KM2A_SPIKE=0`):** the flag-off build is byte-identical on the wire — the same query produced the **identical** `[INFER]` line as the ON build (`"What is a page fault…" -> "A page fault is a crucial concept in **virtual memory manage…"`), `err=0`, **zero `[SPIKE]`/`[RESTART]` lines** (the gated code fully compiles out), no faults. The unconditional `pb_serve_loop` extraction is confirmed behavior-neutral.

**DECISION: Strategy A (reuse-in-place via `pb_restart_entry`) is LOCKED.** Strategy B (revocable dedicated untyped) is not needed. K/M2b promotes this exact mechanic to the real `JARVIS_ACTIONS` respawn wired to fault-EP + heartbeat-age triggers (the parked-worker reuse becomes a multi-worker restart there; a genuinely-faulted worker gets the full launch-triple of §4.2 B). Evidence in the durable/serial log (`[RESTART]`/`[RESTART-PB]` lines). §11 Q1 + Q3 answered; the spike code stays committed (gated 0) as the K/M2b foundation.

### K/M2b-1 RESULT (2026-07-05, KVM `-smp 6`) — multi-worker respawn CORRECT (B + I landed)

K/M2a-2 proved Strategy A for a *quiescent, single-core, PB-main-only* restart. K/M2b-1 lands the worker launch-triple (**B**) + the two-mode probe (**I**) and proves the respawn on the **multi-worker NN=6** box — including a restart fired **WHILE mid-dispatch** (workers active mid-`qmatmul`), the case single-core KVM couldn't exercise. Run under KVM `-smp 6 -cpu host` (`qemu_test.sh` defaults to `-smp 1`, so the direct launch adds `-smp 6`); `numNodes=6` boots 5 workers (`M3: started 5 workers, pb_n_threads=6`).

- **B — worker launch-triple** (`km2b_reset_workers`, `main_x86.c`): per worker, `ReadRegisters → rip=g_pb_worker_entry, rdi=g_pb_remote_wake[i], rsi=i, rdx=g_pb_workers[i].ipc_buffer_addr, rsp=g_pb_workers[i].initial_stack_pointer` (the ABI-aligned launch SP), fs_base preserved, DF clear → `WriteRegisters` → `Resume`. On a PB-main restart the workers' in-flight dispatch is orphaned, so **all 5** are reconstructed to re-enter `jarvis_sel4_worker_entry` (re-park on their wake, re-run `seL4_SetIPCBuffer`).
- **I — probe, two modes.** Mode (a) QUIESCENT (PB parked, workers parked) + mode (b) MID-DISPATCH (a query is kicked off, PA spins a ~400 ms TSC delay so PB is generating with workers active, then the restart fires). `mid=yes` on all 8 mid-dispatch cycles confirms PB was genuinely mid-generation (not an early finish).
- **Measured, BOTH modes, N=8 each (16 restarts):** `dcslot=0` every cycle (cslot-flat, subsumes untyped) · **`workers_reset=5`** every cycle · **`rejoin=OK`** every cycle — the post-restart `jarvis_parallel_for` join (`seL4_Wait(g_pool.done)`) completed, active reached 0, **no deadlock** · coherent Gemma every cycle · `fs_base=kept` · PB-heap pointers **byte-identical across all 16** (`key_cache=0x0623d020`, `layers=0x004a7000`, 17× incl. baseline) · stack high-water 6696 B of 262144 (canary intact) · **zero faults / no musl assert / no deadlock**. **RESULT: BOTH modes PASS.** No new multi-worker landmine surfaced (the wake/done coalescing drain in `pb_restart_entry` + the pool re-init handle the orphaned mid-dispatch cleanly). The `km2b_reset_workers` + two-phase spike are **fully gated `JARVIS_KM2A_SPIKE`** ⇒ OFF byte-identical *by construction* (no deployed-path touch).

**(G + E were deferred from K/M2b-1 as permanent-code work; both are now LANDED — see the K/M2b-2-prereq RESULT below.)**

### K/M2b-2-prereq RESULT (2026-07-05, KVM `-smp 6`) — G + E landed + a THIRD (mid-TOKENIZE) mode proven

K/M2b-2 wires the async fault-EP, which makes E's mid-tokenize malloc window REACHABLE and G's frame-liveness matter over a deployed lifetime — so G + E landed first, as pure refactors (OFF byte-identical) + a gated cooperative flag.

- **G — true file-scope-static hoist (UNGATED).** `qm`/`state`/`vocab`/`tok` moved from `main()` locals to file-scope statics `g_pbm_*` (`inference_server.c`). The `&qm->config` param-collision (§K/M2b-1 note) is dodged cleanly: `main()` aliases the historical names via a **`#define` scoped to `main()` only** (`#undef`'d at its end) — token-based, so `handle_query`'s param sites (before `main`) and `sctx_system_state_t`/`tokenizer_free` are untouched. The K/M2a-2 stash `g_km2a_qm = &qm` now expands to `&g_pbm_qm` (a fixed `.bss` object) — the pointer-into-frame fragility is gone. **OFF `[INFER]` byte-identical** (structs move stack→.bss; deterministic gen). ✓
- **E1 — allocation-free tokenizer (UNGATED).** `tokenizer_encode`'s per-query `sp_text`/`syms` mallocs replaced by scratch pre-allocated once at `tokenizer_init` (`TOK_ENC_MAX_TEXT=2048`; graceful malloc fallback for longer text); `tokenizer_decode` was already alloc-free. Host-verified: `test_tokenizer` **12/12 + ASan/UBSan clean, byte-identical tokens**. This closes the mid-`tokenizer_encode` arena-tear / stranded-lock window. **OFF `[INFER]` byte-identical.** ✓
- **E2 — cooperative restart flag (GATED `JARVIS_KM2A_SPIKE`).** `g_km2a_please_restart` self-checked at the TOP of `pb_serve_loop` (a quiescent point): PB self-drives the reset (drain pool wake/done, `jarvis_sel4_pool_init`, re-signal ready) instead of PA async-Suspending. PA requests it via a `"COOP"` marker. Reserves async Suspend for the wedged/faulted case.
- **Measured, ALL THREE modes N=8 + COOP, on NN=6 (`-smp 6`, 5 workers):** quiescent + mid-dispatch + **mid-TOKENIZE** (mode 2: the `"TOKSPIN"` marker makes PB linger in the tokenize phase so PA suspends there — the window K/M2b-1's mid-generation restart couldn't reach; **`mid=yes` on all 8 TK cycles**). Every cycle: `dcslot=0` · `workers_reset=5` · `rejoin=OK` (join completes, no deadlock) · coherent Gemma · `fs_base=kept` · **PB-heap pointers byte-identical across all 25** (`key_cache=0x0623d020`, `layers=0x004a7000`) · zero faults / no musl assert. **COOP 4/4** self_restart + serve OK. **RESULT: ALL 3 modes + COOP PASS.** With the alloc-free tokenizer, a restart fired **mid-tokenize** has no arena to tear — **no new mid-tokenize landmine surfaced.** **OFF (`JARVIS_KM2A_SPIKE=0`) run: zero `[SPIKE]`/`[RESTART]` lines, `[INFER]` byte-identical to the deploy baseline, zero faults** — G + E1 confirmed pure refactors.

### K/M2b-2 STEP-3 RESULT (2026-07-06, KVM `-smp 6`) — real-crash detect→execute→audit PASS; the STEP-2 re-fault ROOT-CAUSED to a phantom

STEP-2 (commit `a8e3126`) landed the live path and — on a single induced PB-main fault — detected, restarted, and audited correctly, but the post-restart workload then "re-faulted" at a **fixed `ip=678101344` (0x286b0160)** → 5 restarts → the crash-loop bound fired `g_pb_dead`. STEP-3 diagnosed and fixed it. The corrected diagnosis SUPERSEDES both HANDOFF hypotheses:

- **NOT fs_base (hypothesis A — DISPROVEN).** The box shows PB-main `fs_base=0x4704d0` **preserved on every genuine fault** (the null-READ never touches `fs`; `ReadRegisters` reads the live, intact value). §5-B's *conditional* stamp ("stamp IF a real-fault probe shows it clobbered") therefore resolves to **not needed** — preserve-via-ReadRegisters is correct. No stamp added.
- **NOT stale worker/pool context (hypothesis B — DISPROVEN).** Workers reset with valid regs every cycle and the **real worker fault was caught and attributed** (`label=5 badge=1` — the per-worker badged fault-EP mint works). The respawn machinery is sound (heap pointers byte-identical, warm state reused — consistent with the K/M2a-2 16-cycle proof).
- **ROOT CAUSE = `pa_poll_fault` empty-detection.** `seL4_NBRecv` on the fault endpoint with **no message pending does NOT zero the returned MessageInfo** — it leaves stale/garbage register content (box-observed `label≈165553`, stale MR `ip≈0x286b0160`). STEP-2's all-zero heuristic (`label==0 && len==0 && badge==0`) never matched that garbage, so **every poll reported a PHANTOM fault** → spurious restart. The STEP-2 "re-fault ip=678101344" was literally that stale MR — never real code (`0x286b0160`'s top bits are the garbage label `0x286B0`). **Fix:** gate on a valid fault-tag label — the kernel `DebugAsserts` every `seL4_Fault` type fits in 4 bits (`NullFault=0 … VMFault=5`), so `1..15` = real fault, `NullFault(0)` / any label `>15` = endpoint empty (`main_x86.c` `pa_poll_fault`). One-line root-cause fix; all diagnostics kept.
- **STEP-3 additions:** the worker-fault probe (`WFAULTPROBE` + a stack-anchor selector that guarantees a WORKER, not the dispatcher, takes the poison) + per-worker **badged** fault-EP caps (badge=i attribution, whole-PB restart policy unchanged); the recovery gate upgraded to a **real cache-MISS worker-dispatched inference with the text captured** (STEP-2's bare `rc==0` couldn't tell a coherent gen from any `MSG_RESPONSE`); and the **windowed crash-loop bound WIRED** — STEP-2 defined `KM2B_HEALTHY_RESET=25` + zeroed the counter on restart but never incremented it, so the "windowed" bound was a *lifetime* cap (any 5 lifetime self-heals → permanent `g_pb_dead`); now a completed inference ticks `g_healthy_since_restart` and the window resets after 25 healthy inferences (true sliding window). The keyword-clean trigger builder was extracted host-pure (`km2b_trigger.c`, `test_km2b_trigger.c` 11/11 CI-gated).
- **Gate run (ACTIONS=1, ACTION_PROBE=1, CACHE_GROWTH=0 so q_infer is observable):** **5/5 PB-main + 5/5 worker induced fault cycles ALL `recovery=COHERENT`**; `[FAULT]` total = 10, **phantoms = 0** (`label≠5` count = 0), restarts EXECUTED = 10 (one per fault), **0 `[FATAL]`** (never tripped the bound), coherent Gemma throughout (`"The seL4 microkernel is a **formally verified, high-assurance…"`), `err=0`. Interim fixed-build run on the **deployed config** (CACHE_GROWTH=1): `q=34,800 err=0`, exactly 2 induced faults, **0 phantoms** — the fix does not regress the deployed path. **OFF (`JARVIS_ACTIONS=0`) process-config byte-identical** (all live-path code is `#if JARVIS_ACTIONS`/`JARVIS_ACTION_PROBE`/`JARVIS_RESPAWN`; the always-compiled `km2b_trigger.c` is never *called* OFF). **RESULT: real PB-main AND worker faults both detected → whole-PB restart → coherent worker-dispatched recovery → JACT audit, N≥5 each, no re-fault, no crash-loop.** The STEP-2 phantom re-fault is GONE.

### K/M2c RESULT (2026-07-07, KVM `-smp 6`) — the HANG/wedge lane + first off-box JACT read-back PASS

STEP-3 closed the CRASH lane (PB faults → fault EP → restart). K/M2c closes the OTHER failure mode: PB **alive but WEDGED** (no fault fires, the fault EP stays silent), detected by a shared consecutive-PB-contact-miss counter that funnels the SAME `pa_restart_pb("hang", …)`.

- **Detection = a host-tested pure state machine.** `km2b_miss.c/h` (`km2b_miss_t g_pb_miss`): `km2b_miss_on_pb_timeout(lane)` advances, `km2b_miss_on_pb_ack` resets to 0, `km2b_miss_tripped(threshold)` is `count >= threshold`. The counter moves ONLY on explicit calls (no "absence looks like an event" — the STEP-3 phantom class). Wired at the 3 PB-contact lanes (all `#if JARVIS_ACTIONS`): inference PURE-timeout (reached only AFTER `pa_fault_check` found no fault → crash and hang never double-count), heartbeat/shield `wait_for_response != 0`; RESET on any genuine typed dequeue (`got_response` / `wait_for_response == 0`), NEVER a bare `seL4_Wait`. Cache lane + `cg_served_now` call NEITHER (D4 neutrality). The single post-lane trigger at `next_query` (`!g_restart_in_progress && !g_pb_dead`) reuses `km2b_do_respawn` + the crash-loop window + the JACT audit unchanged. `test_km2b_miss.c` **20/20**, CI-gated. The keyword-clean trigger builder (`km2b_trigger.c`, host-tested for `"hang"`) formats the JACT `trigger_snapshot`.
- **Heartbeat-age is INSTRUMENT-ONLY (D1).** Each genuine HB-ACK logs the observed inter-HB gap (`[HB-AGE] gap_ms=…`, suppressed while `g_infer_active`); the box shows small gaps (~21–67 ms). The calibrated age-backstop is deferred to K/M3+ (its threshold is uncalibrated per §11-Q5); the miss-counter deterministically catches the wedge.
- **The wedge must DESCHEDULE, but a hard busy-loop may STILL be detected (UNVERIFIED — corrected 2026-07-07).** PA and PB-main share core 0 (only the M3 workers pin to cores 1–5). The first `HANGPROBE` attempt (`for(;;) pause`) hung the probe, so `HANGPROBE` now wedges with `for(;;) seL4_Yield()` — a realistic **soft-hang** (alive, scheduled, never serving). BUT the "a hard same-core busy-loop is undetectable" conclusion I first wrote rests on a **probe-harness artifact**, not the production path: the box gate detected the wedge through the probe's short inline recv, and the deadlock likely came from the harness, not from PA starving. Under this build (all threads `seL4_MaxPrio`, **DOMAINS OFF, non-MCS**), standard equal-priority **round-robin means the timer tick preempts PB-main's timeslice and rotates core 0 to PA even if PB-main never yields** — and PA's production lanes **POLL** (`wait_for_response`, never `seL4_Wait`), so the miss counter would keep counting down and trip on a hard loop too. So whether a hard same-core busy-loop is genuinely undetectable is **OPEN** — likely it IS detectable via the production polling path. This is upside, not a defect, but K/M4 must NOT codify "power-cycle only for hard loops" on this unproven premise. **A real hard-loop box experiment** (a non-yielding `for(;;)` in PB-main; observe whether `g_pb_miss` trips via the production `wait_for_response` lanes) is a **K/M4 pre-flip carry-forward** (§10). The probe's HB detection uses a short probe-local wait (PB is known-wedged) so the N-cycle gate stays tractable; the production lanes use the real 5M-poll `wait_for_response`.
- **Follow-up fixes (a0dde19 → next commit, both gated, audit/config-fidelity — no deployed-path change).** An adversarial verify of `a0dde19` found two MINOR defects: **D1** — the inference-lane `PB_DISPATCH_OK()` §5-F guard was nested in `#if JARVIS_CACHE_GROWTH`, so an `ACTIONS=1 && CACHE_GROWTH=0` build sent `MSG_QUERY` UNGUARDED after `g_pb_dead` (the "dispatch STOPPED" log was a lie for that lane; it only slipped through because the gate never tripped `g_pb_dead`). Fixed: `cg_served_now` is now declared unconditionally and the guard wraps the inference send regardless of cache-growth (all 4 `ACTIONS×CACHE_GROWTH` combos compile). **D2** — the crash-vs-hang no-double-count fault poll existed ONLY on the inference lane; a PB crash during an HB/shield wait could JACT-record `"respawn hang"` for a real crash. Fixed: the `next_query` hang-trip now calls `pa_fault_check()` FIRST — a queued fault audits as `"fault"` and the `"hang"` restart is skipped (recovery was always fine; this is JACT/SEC-039 evidence-fidelity). `test_km2b_miss.c` +T11/T12 (NULL-arg + threshold-0 guard) = 22/22.
- **Adversarial review (5-lens workflow) found + fixed the §5-F degraded-dispatch gap.** After `g_pb_dead` trips, STEP-2/3 logged "PB dispatch STOPPED" but the 3 lanes never checked `g_pb_dead` — so a dead PB kept getting ~60–120 s timeouts + inflated `q_errors` + churned the miss counter. Fixed with `PB_DISPATCH_OK()` (compile-time `1` when `JARVIS_ACTIONS=0` → folds out → byte-identical) guarding all 3 lanes, making the "cache-only" claim TRUE. (2 other findings dismissed with reasoning: a bounded ≤1-cycle stale-response reset within threshold tolerance; and an unreachable out-of-inference-crash misclassification.)
- **Gate (ACTIONS=1, ACTION_PROBE=1, CACHE_GROWTH=0):** **5/5 PB-main crash + 5/5 worker crash + 5/5 HANG cycles ALL `recovery=COHERENT`**; every hang cycle `miss_count=3 fault_EP=silent -> TRIPPED` → `[RESTART] reason=hang risk=10 EXECUTED outcome=OK`; **0 phantoms**, **0 `[FATAL]`**; `q_infer` advancing (13→17→30), `err=0`; `[HB-AGE]` live. **First off-box JACT read-back** (`dd skip=21120000 count=4097 | parse_action_audit.py`, checksum OK): real records show **10 `respawn fault` + 5 `respawn hang lane=2 miss=3 age=0ms`**, all `action=1 NOTIFY/EXECUTED/OK risk=10`, keyword-clean, monotonic `boot_id:seq`. Untyped/cslot-flat is inherited from the identical `km2b_do_respawn` (K/M2a-2's 16-cycle 3-axis-flat proof; `[RESTART-PB]` heap pointers byte-identical every cycle, `key_cache=0x0623d020`). **OFF (`JARVIS_ACTIONS=0`) process-config byte-identical** (miss wiring / hang trigger / `[HB-AGE]` / `PB_DISPATCH_OK` all fold out). **RESULT: crash AND hang both self-heal via a coherent worker-dispatched recovery, the wedge (silent fault EP) is caught by the miss-counter, and the JACT trail reads back off-box.**

## 6. The ordered respawn sequence

1. **Suspend PB-main + ALL workers FIRST** (`seL4_TCB_Suspend`). A live worker mid-`seL4_Wait` on a since-freed wake cap faults on unmapped memory if you teardown before suspending.
2. **Apply the chosen strategy** — A: reset `.data`/zero `.bss` + set PC/SP; B: `seL4_CNode_Revoke` + re-retype the dedicated untyped.
3. **`shmem_ipc_init` BOTH rings** — allocation-free (memsets the existing mapped page + stamps `SHMEM_MAGIC 0xDEADBEEF` / version / `SHMEM_RING_SLOTS 15`, `shmem_ipc.c:47-53`). Clears torn / half-written messages + indices on the live frames. (`shmem_ipc_reset`, `:171`, zeros indices only — insufficient if slot contents are stale.)
4. **Do NOT `sctx_init`** — preserve PA working memory. PA is the single writer, so the SCTX seqlock is even/consistent across a PB crash; re-initializing would drop live context.
5. **Drain `resp_notif` + `wake[]`/`done`** — seL4 Notifications **COALESCE** (`threadpool_sel4.c:109-112` flags this exact hazard). A stale signal desyncs the M3 join (last-worker-to-zero mis-fires) or the ready handshake.
6. **Clear the PA latch + `infer_active`** — a mid-inference crash leaves a phantom `infer_active=1` (a fake in-flight inference / phantom throughput on the console); also discard any orphan `MSG_INFER_STATS` (§7).
7. **(B only) re-copy caps + re-map model/shared into the new VSpace + rebuild argv** (`main:1424-1440`).
8. **Reset/recreate workers** — re-supply each worker's **`ipc_buf` as the 3rd entry arg** (`seL4_SetIPCBuffer` is the worker's first act, `threadpool_sel4.c:45`; omitting it faults the first `seL4_Wait` at null-buffer — the M3 bring-up bug); re-apply `seL4_TCB_SetAffinity` (B); re-run `jarvis_sel4_pool_init` so `g_pool` is fresh.
9. **Ready handshake = DRAIN `resp_notif` then POLL the response ring for `MSG_HEARTBEAT_ACK 0x04` with a TIMEOUT — NEVER a bare `seL4_Wait(resp_notif)`.** The first boot uses the one legit `seL4_Wait(resp_notif)` (`main:2761`), but after a crash `resp_notif` may already be stale-signaled → the drain-then-poll pattern (`wait_for_response`, whose comment `:1459` documents the ~7% spurious-error Heisenbug) is mandatory. PB re-signals ready only AFTER model-load + M3 pool re-init (`inference_server.c:672-690`), so workers must be re-created before the ACK. A failed respawn ⇒ audit outcome **FAIL** + do NOT deadlock (bounded poll, then surface).
10. **Resume the workload** (`goto next_query`-equivalent), preserving the duty-cycle / FB-counter bookkeeping the existing `next_query` path (`main:3614`) does — a restart hook inserted before it must not skip that.

**K/M2b-2 refinements to this sequence (§5-B/D, 2026-07-06):** (i) the whole sequence runs inside a single `pa_restart_pb(reason)` funnel guarded by a **`g_restart_in_progress` latch** — the fault-poll + hang trigger are skipped while it is set, so a fault that arrives mid-respawn cannot re-enter (the JACT write + `restart_count` bump fire EXACTLY ONCE per resolved event). (ii) **Suspend-FIRST** each thread (the real fault leaves it `BlockedOnReply`; a bare Resume no-ops). (iii) **Drain the fault endpoint** (`seL4_NBRecv` to empty) AFTER Resume — the register-rewrite already "handled" the fault, so never `seL4_Reply`; draining clears any queued sibling faults so the next poll starts clean.

## 7. Landmines (each with its fix + citation)

- **Stale `resp_notif` signal** (`main:2761` legit-wait vs `:1459` Heisenbug) → drain-then-poll-with-timeout at the ready handshake (step 9); never bare `seL4_Wait` on the runtime path.
- **Ring-reset-YES / SCTX-reset-NO asymmetry** → re-init both rings (step 3), preserve SCTX (step 4). The rings carry cross-process in-flight IPC (torn on a crash); SCTX is PA-single-writer (consistent).
- **Worker teardown ordering** (`main:1398` local `wt`, not process-owned) → suspend-before-teardown (step 1); track workers in a persistent array; drive them to the clean `g_pool.shutting_down` exit (`threadpool_sel4.c:113-117`, quiescent-only today) before destroying TCBs.
- **Notification coalescing** (`threadpool_sel4.c:109-112`) → drain/re-create the reused wake/done caps (step 5) so a coalesced stale wake can't let a worker skip its active-counter decrement and hang the join.
- **Mid-inference orphan response + `MSG_INFER_STATS` half-latch** (`shmem_ipc.h:40-42`) — NOT a torn message; an ORDERING hazard: PB crashes after sending `MSG_INFER_STATS 0x11` but before/during the `MSG_RESPONSE` chunks → PA latches a tok/s for an inference that produced no output. Fix: the crashed query's poll hits POLL_TIMEOUT → abandon it as FAILED; the ring re-init (step 3) discards the orphan stats + partial response.
- **Orphan in-flight request** (PA sent `MSG_QUERY`, PB died before consuming) → cleared by the request-ring re-init (step 3).
- **`restart_pb` self-blocking on its own trigger text** (`shield_action.c:87-94`) — restart_pb is unblockable by risk SCORE (SELF_HEAL 10 + max learned 50 = 60 < threshold 80) **but IS blockable by the keyword blocklist**. The fault/hang `trigger_snapshot` string MUST avoid the 9 canonical keywords (`kill`, `shutdown`, `halt`, `destroy`, `delete`, `remove`, `format`, `rm -rf`, `drop table`). Use e.g. `"PB fault ep"` / `"hb age 34000 ms"` — never `"PB was killed"`.
- **`trust_policy` not yet wired live** (`shield_action.c:121-133` defined/tested; never called in `main_x86.c`) → K/M2 adds the first live call; state it's a NEW step, not a reuse.
- **argv/cap regeneration** (`main:1424-1440`) → **Strategy B** rebuilds the exact same argv (notif cptrs, worker wake/done cptrs, model/shmem vaddrs, `pb_n_threads`) after re-copying caps into the new CSpace. **Strategy A** preserves the caps (no regeneration) but re-executes `main()` at the reset PC/SP, so it must guarantee the **argv strings survive at the re-entry SP** (PB parses them at `inference_server.c:431-434,467-470,676-682`) — a spike question (§11 Q3).

## 8. Audit + telemetry

On every restart decision PA writes a JACT record (`act_audit_append(&g_action_audit, …)`, `main:2264` pattern; store `main:503`, init `:2141`): `action_id=ACTION_RESTART_PB`, `verdict=AUDIT_EXECUTED` (or `AUDIT_BLOCKED` if SHIELD ever refuses — it won't for a keyword-clean trigger), `outcome=AUDIT_OUT_OK|AUDIT_OUT_FAIL` (respawn succeeded / failed), `risk_x100` (the `shield_assess` score), `trigger_snapshot` = the fault/hang detail (keyword-clean, e.g. `"PB fault ep"` / `"hb age 34000 ms"`) — plus `restart_count++`. **The write path is entirely PA-side** (`action_audit.c` is pure + callback-driven over PA's own NVMe bounce; `evidence: no seL4/IPC/PB symbol`), so it logs even while PB is down/restarting. `restart_count` becomes the **K/M3 telemetry v7** field (with `actions_fired`/`actions_blocked`); the audit store is the 7-day criterion's evidence base ("no Level-2+ actions without approval" is answered by reading it via `parse_action_audit.py`).

## 9. Fault-injection probe (K/M2 extension of `JARVIS_ACTION_PROBE`)

TWO separate, probe-gated inducers so **both** detection paths are proven:
- **Crash** (exercises the fault EP): probe-gated code INSIDE PB deliberately faults (e.g. a null deref or `seL4_TCB_Suspend`-then-invalid-op) → PA's fault-EP poll fires.
- **Hang** (exercises the heartbeat-age path): probe-gated PA-side `seL4_TCB_Suspend(PB-main)` (or a PB busy-loop) → PA's `last_hb_ack_ms` age crosses the threshold.

Each must end in a restart + a JACT record + **resumed coherent inference**. Keep them behind `JARVIS_ACTION_PROBE` (default 0) so deploy is inert.

## 10. Milestone decomposition + done-when

- **K/M2a — the spike + strategy-lock** (KVM): implement both A and B respawn skeletons minimally, measure free-untyped across N restarts, answer the §11 open questions, **lock A or B**, and get this design reviewed. No committed behavior change. ✅ **DONE** — direction locked (A), mechanic refined (`pb_restart_entry`, §4.1).
- **K/M2a-2 — hardened `pb_restart_entry` + corrected zero-RESOURCE measurement** (KVM), gated `JARVIS_KM2A_SPIKE` (default 0). ✅ **DONE 2026-07-05 — Strategy A LOCKED** (§4.2 pre-mortem hardening + RESULT: N=16 flat on all three axes, coherent, `fs_base` preserved, OFF byte-identical). Committed as the K/M2b foundation.
- **K/M2b-1 — multi-worker respawn (land B + I) proven on NN=6** (KVM `-smp 6`), gated `JARVIS_KM2A_SPIKE`. ✅ **DONE 2026-07-05 — §4.2 K/M2b-1 RESULT:** the worker launch-triple (`km2b_reset_workers`, B) + the two-mode probe (quiescent + MID-DISPATCH, I) proven — 3-axis flat + workers rejoin + coherent gen across N=8 in BOTH modes, no fault/deadlock/musl-assert. **G (true file-scope hoist) + E (cooperative flag / alloc-free tokenizer) SCOPED to K/M2b-2** (see the RESULT note — the mid-generation restart is malloc-free so E's arena-tear window is unreachable here, and the `&qm->config` param-vs-global collision makes a safe G rename a permanent-code job; the proven pointer-stash is fragility-free by the 16-cycle heap-identity result).
- **K/M2b-2-prereq (G + E)** ✅ **DONE 2026-07-05** (§4.2 K/M2b-2-prereq RESULT) — the respawn is permanent-ized (file-scope statics + alloc-free tokenizer + cooperative flag; OFF byte-identical; all 3 restart modes incl. mid-tokenize proven on NN=6).
- **K/M2b-2 — live fault/hang detect → first live `shield_assess` → execute respawn → JACT audit** (behind `JARVIS_ACTIONS`, default 0). **Spec CORRECTED 2026-07-06 (5-agent pre-mortem, §5 A–G):** the fault EP already exists (§11 Q2 closed) — the work is the fault RECEIVER (`seL4_NBRecv`, poll-in-spin) + net-new worker fault handlers + the detection redesign (heartbeat-age suppressed while `g_infer_active`, ONE shared miss-counter fed by the 3 PB-touching timeout sites, cache-lane-neutral) + the single `pa_restart_pb` funnel (`g_restart_in_progress` latch) + `shield_assess(learn=NULL)` with a system-facts-only keyword-clean `trigger_snapshot` + JACT audit + the windowed crash-loop bound → `g_pb_dead` degraded state. **STEP-2 IMPL DONE 2026-07-06 (`a8e3126`, gated).** ✅ **STEP-3 REAL-CRASH GATE PASSED 2026-07-06 (§4.2 STEP-3 RESULT):** the STEP-2 post-restart "re-fault" was ROOT-CAUSED to a `pa_poll_fault` phantom (`seL4_NBRecv` on an empty EP returns garbage, not zero — NOT fs_base, NOT worker context; both HANDOFF hypotheses disproven), fixed by gating on a valid fault-tag label (`1..15`); 5/5 PB-main + 5/5 worker induced faults ALL recover COHERENT, 0 phantoms, 0 `[FATAL]`, `err=0`; the windowed crash-loop bound wired (was a lifetime cap); OFF process-config byte-identical.
  - **Corrected gate wording:** `err=0` alone is NOT PB-alive proof — cache-growth serves repeats so a *dead* PB reads `err=0` with `q_infer` frozen. The real gate is **`q_infer` ADVANCING** (PB provably serving) over the run, proven with `CACHE_GROWTH=0` so inferences hit PB directly; `err=0` over the deployed-config high-volume run (`q=34,800`) is the separate volume/no-regression leg.
- **K/M2c — box smoke:** induce **crash AND hang separately** → PA restarts PB → the JACT record reads back via `parse_action_audit.py` → coherent inference resumes → `err=0` → **N repeated restarts do NOT drain untyped** (the §2 exhaustion gate). OFF-vs-ON `[INFER]` byte-identical (flag-OFF). ✅ **DONE 2026-07-07 (§4.2 K/M2c RESULT):** the hang/wedge lane (shared miss-counter `km2b_miss.c/h`, host-tested 20/20) + the first off-box JACT read-back. Box gate: 5/5 crash-PB-main + 5/5 crash-worker + 5/5 HANG all `recovery=COHERENT` (every hang `fault_EP=silent miss=3 TRIPPED`), 0 phantoms, 0 `[FATAL]`, `q_infer` advancing, `err=0`; `dd|parse_action_audit.py` shows 10 `respawn fault` + 5 `respawn hang` EXECUTED records (checksum OK); untyped/cslot-flat inherited from the identical `km2b_do_respawn` (K/M2a-2's 16-cycle proof); OFF process-config byte-identical. A 5-lens adversarial review found + fixed the §5-F degraded-dispatch gap (`PB_DISPATCH_OK()`); a later verify banked two more gated audit/config-fidelity fixes (D1 the CACHE_GROWTH-coupling of the inference guard; D2 crash-vs-hang audit classification on the HB/shield lanes). Heartbeat-age is instrument-only (`[HB-AGE]`); the calibrated age-backstop is K/M3+ scope. **K/M4 pre-flip carry-forwards (these gate the SEC-039-closing flip):** (1) a hard-loop box experiment — whether the production polling path detects a non-yielding PB-main busy-loop (likely YES via timer-tick round-robin; the "undetectable" claim is UNVERIFIED, see §4.2); (2) `pa_poll_fault` label-gate defense-in-depth (badge ∈ 0..N and/or MR0 plausibility, from the STEP-3 review); (3) weigh a dedicated PA core only if (1) shows hard loops are genuinely undetectable (likely unnecessary).

- **K/M3 — telemetry v7 (the `restart_count` telemetry signal the done-when names).** ✅ **DONE 2026-07-07:** `uint32 restart_count` + `uint16 actions_fired` + `uint16 actions_blocked` + `TLM_F_ACTIONS` 0x800 → 232 B, CRC@228, version 7 (finalize is offsetof-based → no `.c` change). Full lockstep landed in one commit (header → `telemetry_receiver.py` → `telemetry_fixture.py` → `gen_golden_pcap.py` → `golden.pcap` → console `System` "Self-healing" stats + a Capabilities auto-row; the SHIELD screen's passive-ALLOW note scoped to the QUERY path so it never contradicts a nonzero `actions_blocked` — the SEPARATE action gate). All fill `#if JARVIS_ACTIONS` (default 0) → the deploy emits 0s + `TLM_F_ACTIONS` clear + `[INFER]` byte-identical (the SAME gated-honest pattern as the v5 shield-learn / v6 semantic slices). Host green: `test_jarvis_telemetry.c` 55/55 (sizeof 232 + the 3 offsets + `TLM_F_ACTIONS`==0x800 + survive-finalize), `test_telemetry_receiver.py` 119/119 (FMT `…6IHHHHIHHI`, the 3 keys, `ACTIONS` flag symmetry, `actions_blocked` allowed vs `shield_blocked` banned), console honesty 63/63, golden-drift regenerated. Box smoke (transient ACTIONS=1): the counters climb via `[RESTART]`/`[ACTION-PROBE]` + a `[TLM-V7]` summary line; on-wire I211 validation of nonzero v7 is deferred to the K/M4 flip (no NIC in QEMU — like v5/v6).

**Done-when (canon B1, `ROADMAP.md` §Backlog):** an induced PB crash on the box (or QEMU) auto-restarts PB, service resumes with err rate unaffected (measured as **`q_infer` advancing + `err=0`**, NOT `err=0` alone — cache-growth masks a dead PB), and the event is durable + reconstructable from the NVMe log + a `restart_count` telemetry signal. **✅ MET across K/M2b-2 STEP-3 (crash) + K/M2c (hang) + K/M3 (the `restart_count`/`actions_*` v7 wire signal).**

## 11. Risks + open questions (the spike answers these)

1. **Does the leaky allocman reclaim ANYTHING on a reset?** (Verified: no — so Strategy A's "draw zero untyped" is the safe path; B needs net-new `seL4_CNode_Revoke`.)
2. ✅ **CLOSED (2026-07-06).** The fault EP is not just available — it is ALREADY created + PB-main bound: `process_config_default_simple` → `process_config_create_fault_endpoint` (`create_fault_endpoint = true`), `inference_process.fault_endpoint` allocated, PB-main bound @ `SEL4UTILS_ENDPOINT_SLOT` (non-MCS). K/M2b-2's job is the RECEIVER (`seL4_NBRecv`) + net-new worker handlers, not "add an EP" (§5 corrected).
3. **Is Strategy A's ELF re-init tractable** — does `sel4utils` re-load into existing frames, or must PA hand-roll `.data` memcpy / `.bss` memset from the mapped CPIO ELF?
4. **Does revoking PB's instance untyped (B) SPARE the model + shared frame caps?** (Requires them in a separate untyped — must be proven, else B destroys the warm model.)
5. **The correct heartbeat-age threshold + N** — margin above the ~12 s worst-case single-query latency without false-positiving a busy PB.
6. **Worker teardown/reset mechanics** — `sel4utils_clean_up_thread` vs manual TCB/stack/IPC-buffer free; the clean `g_pool.shutting_down` drive-to-exit ordering.

## 12. Honest ceiling

> K/M2 is **bounded self-healing, not fault tolerance.** PA restarts a crashed/hung PB from the CPIO with the warm model preserved — but a **max-restart bound** applies: if PB keeps crashing (a crash-loop), PA **stops restarting, surfaces the condition** (audit + telemetry + HUD), and does NOT restart-loop. The worst case of a single restart is what a power-cycle already does (PB holds no durable state); the decision is SHIELD-scored and every attempt is durably audited. It stays **gated `JARVIS_ACTIONS` default-0 — deploy-inert until the K/M4 flip decision** (like every prior flip: real signal + box proof first). No claim of unattended reliability; the 7-day supervised run (goal 6-7) is where sustained self-healing is actually demonstrated.
>
> **K/M2b-2 refinement (§5-F, 2026-07-06):** the bound is a **windowed** consecutive-restart counter (SEPARATE from the lifetime `restart_count`), distinguishing "restart executed + PB came back healthy" (reset after K sustained-coherent queries — a possible false-positive) from "restart failed / PB re-faulted immediately" (bound tightly). Critically, "surface + stop" must stop the doomed **sends**, not just the restarts: on the bound, set `g_pb_dead` → the workload loop STOPS dispatching to PB (serves cache-only, skips the MSG_QUERY/HB/SHIELD sends so it never re-enters the 5M-poll timeout) + raises a distinct `[FATAL]` HUD/telemetry state. Otherwise a permanently-dead PB would burn every query into a ~60 s timeout, masquerading as a hang.

---

*Companion to `phase6/docs/PHASE_6_GOAL_K_IT_ACTS.md` (K/M2). Ground truth re-verified against HEAD `4d77a7e` (2026-07-05). The A-vs-B choice is deferred to the K/M2a spike — this doc locks the constraint (reuse-in-place), the preserve/teardown boundary, the detection design, and the ordered sequence, not the strategy.*
