# Tracked files that build_jarvis_x86.sh FULLY REWRITES at build time, and which are
# therefore excluded from tracked-hashes.txt (that baseline describes the tree BEFORE
# the build script runs; the box's copy is post-build and legitimately differs).
#
#   src/main.c  <-  copied verbatim from phase3/src/sel4/main_x86.c
#
# NOTE apps/sel4test-driver/CMakeLists.txt is NOT in this list, though it was until
# 2026-08-09. That was WRONG and the boot smoke found it: the build script only seds
# ONE line of it (the AVX2 target_compile_options), while the file itself carries 96
# insertions / 69 deletions of hand-made JARVIS delta -- it strips the
# add_subdirectory() calls for libsel4testsupport and sel4test-tests, the very dirs
# the 72-path deletion list removes. Excluding it made the reconstruction produce a
# tree that cmake could not configure. It is vendored in files/ instead, in its
# post-sed state (the script's sed is idempotent, so re-running it is a no-op).
apps/sel4test-driver/src/main.c
